# LANDMARKS

Verified anchors and reusable refutations for `amqp-ng`.

## Active Landmarks

- [LM-001|loc] The repository has implementation code and specs, not only design prose. Evidence: `rg --files` shows `src/amqp*.cr` and `spec/**/*.cr`; `README.md` now says v0.1.0 has a working client, specs, benchmark harness, and release branch. Trust `{F:0.9,G:0.8,R:0.9}`. Status: verified 2026-05-16; decay trigger: README/status rewrite or implementation removal.

- [LM-002|loc] No root `TODO.md` or `LANDMARKS.md` existed before this ledger was created. Evidence: `find . -maxdepth 1 \( -name 'TODO.md' -o -name 'LANDMARKS.md' \) -print` returned empty before creation. Trust `{F:1.0,G:1.0,R:1.0}`. Status: verified 2026-05-15; decay trigger: this file pair exists now.

- [LM-003|pattern] The docs contain many normative falsifier IDs, but the live spec tree is not organized by the documented matrix paths such as `spec/api/`, `spec/publisher/`, or `spec/reliability/`. Evidence: `docs/16-falsifier-matrix.md` names those paths; `rg --files spec` shows flat/current spec layout. Trust `{F:0.8,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: spec tree restructuring.

- [LM-004|refute] README honesty drift was corrected: the README no longer claims pre-implementation and now reports active v0 implementation plus current default verification and opt-in broker gates. Evidence: `README.md`; current no-broker `crystal spec --error-trace` baseline is 229 examples after the TLS wrong-SAN guard pass. Trust `{F:0.9,G:0.8,R:0.9}`. Status: refreshed 2026-05-19; decay trigger: README/status rewrite, implementation removal, or verification baseline change.

- [LM-004B|guard] Declared content body overflow is now executable evidence, not only a docs promise: `spec/channel_spec.cr` feeds synthetic basic.deliver, content-header, and body frames through the real channel frame-processing path and verifies overflow beyond the declared body size raises `Amqp::ProtocolError`. Evidence: release and dev Crystal focused `crystal spec spec/channel_spec.cr --error-trace` both pass with 28 examples; full no-broker `/opt/homebrew/bin/crystal spec --error-trace` passes with 228 examples. Trust `{F:0.9,G:0.65,R:0.9}`. Status: verified 2026-05-18; decay trigger: channel content assembly, content-header decoding, or frame processing changes.

- [LM-004C|guard] Nested AMQP field containers are now bounded at 32 recursive table/array levels on both decode and encode. Decode overflow raises `Amqp::ProtocolError`; encode overflow raises `Amqp::ConfigurationError`. Evidence: `src/amqp/wire/amqp_zero_nine_one/types.cr`, `spec/wire/types_spec.cr`; release and dev Crystal focused type specs pass with 16 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-18; decay trigger: field codec recursion or `Amqp::FieldValue` model changes.

- [LM-004D|guard] Inbound content bodies are now bounded by `Config#max_body_size`, defaulting to 64 MiB. `Channel#process_header_frame` rejects a broker-declared `body-size` above the cap before any body frames are buffered; URI and keyword config values are guarded as positive `UInt64`. Evidence: `src/amqp/config.cr`, `src/amqp/channel.cr`, `spec/config_spec.cr`, `spec/channel_spec.cr`; release and dev Crystal focused config/channel specs pass with 41 examples. Trust `{F:0.9,G:0.68,R:0.9}`. Status: verified 2026-05-19; decay trigger: config parsing, channel content assembly, or content-header handling changes.

- [LM-004M|guard] In-flight content body assembly is now bounded per connection by `Config#max_inflight_body_bytes`, defaulting to four times `max_body_size`. Channels reserve declared body bytes at content-header time and release on successful assembly, overflow, close, or recovery reset. This does not bound delivered consumer mailbox bytes or process-wide memory. Evidence: `src/amqp/config.cr`, `src/amqp/connection.cr`, `src/amqp/channel.cr`, `spec/config_spec.cr`, `spec/channel_spec.cr`; focused config/channel specs pass with 46 examples, docs falsifier spec passes with 4 examples, full no-broker spec passes with 234 examples, and later full live default suites pass against RabbitMQ 3.13.7 and LavinMQ 2.4.0 with 237 examples each. Trust `{F:0.9,G:0.68,R:0.9}`. Status: verified 2026-05-20; decay trigger: config parsing, content assembly, channel close/recovery reset, consumer mailbox ownership, broker version, or Crystal version changes.

- [LM-004N|guard] Worst-case subscription mailbox body bytes are now bounded by `Config#max_subscription_mailbox_bytes`, defaulting to `1024 * max_body_size` to preserve the existing low-level consume buffer default. Subscription registration rejects `buffer * max_body_size` above the cap and rejects negative buffers. This is capacity-based, not actual queued-byte accounting. Evidence: `src/amqp/config.cr`, `src/amqp/channel.cr`, `spec/config_spec.cr`, `spec/channel_spec.cr`; focused config/channel specs pass with 49 examples, docs falsifier spec passes with 4 examples, full no-broker spec passes with 237 examples, and full live default suites pass against RabbitMQ 3.13.7 and LavinMQ 2.4.0 with 237 examples each. Trust `{F:0.9,G:0.68,R:0.9}`. Status: verified 2026-05-20; decay trigger: config parsing, consume buffer defaults, subscription mailbox implementation, actual queued-byte accounting introduction, broker version, or Crystal version changes.

- [LM-004E|guard] URI query-key docs now have an executable drift guard: `spec/docs_falsifier_link_spec.cr` compares `docs/04-uri-and-config.md` and README query-key lists with `Amqp::Config::RECOGNIZED_QUERY_KEYS`. Evidence: release and dev Crystal focused docs falsifier spec pass with 4 examples; current full no-broker baseline is 229 examples. Trust `{F:0.9,G:0.75,R:0.9}`. Status: verified 2026-05-19; decay trigger: config query surface or public config docs changes.

- [LM-004F|guard] Checked-in no-broker CI now covers pinned Crystal 1.19.2 and 1.20.2 for `shards install`, format, full no-broker specs, and no-codegen tool builds. Evidence: `.github/workflows/ci.yml`; local workflow YAML parse passes; local matching 1.20.1 release gate passes with 229 examples. Trust `{F:0.78,G:0.62,R:0.74}`. Status: configured 2026-05-19; decay trigger: GitHub Actions runner image, `crystal-lang/install-crystal`, Crystal release pins, or CI workflow changes.

- [LM-004G|guard] TLS hostname verification now has a default no-broker wrong-SAN witness. `spec/tls_spec.cr` generates a trusted self-signed certificate for `wrong-host.test`, serves it from a local TLS socket, connects with URL host `127.0.0.1`, and requires `Amqp::TlsHandshakeError`. Evidence: focused TLS spec passes with 5 examples and 1 live-broker pending; full no-broker spec passes with 229 examples. Trust `{F:0.88,G:0.65,R:0.88}`. Status: verified 2026-05-19; decay trigger: TLS wrapping, `hostname:` handling, OpenSSL stdlib behavior, or TLS spec fixture changes.

- [LM-004H|guard] Checked-in plain-AMQP broker CI now covers RabbitMQ 3.13.7 and LavinMQ 2.4.0 on Crystal 1.20.2. The workflow waits for `AMQP_URL` reachability and runs the full spec suite against each broker; the live-gated TLS/backpressure/chaos/performance specs stay outside push/PR CI. Evidence: `.github/workflows/ci.yml`; local Docker smokes for RabbitMQ and LavinMQ both pass with 237 examples and 4 opt-in pending specs after the memory-capacity guards. Trust `{F:0.84,G:0.62,R:0.84}`. Status: refreshed 2026-05-20; decay trigger: broker images, GitHub service-container networking, Crystal pin, live spec env gating, or memory-capacity guard changes.

- [LM-004I|guard] Manual perf-smoke CI now executes `tools/perf_publish.cr` against RabbitMQ 3.13.7 and LavinMQ 2.4.0 with tiny default counts, validates required positive JSON lanes with `tools/perf_smoke_assert.cr`, and uploads JSON artifacts. This is a benchmark-harness health check, not a normative throughput threshold. Evidence: `.github/workflows/perf-smoke.yml`, `tools/perf_smoke_assert.cr`; local low-count RabbitMQ and LavinMQ perf smokes exit 0, emit JSON, and pass assertion. Trust `{F:0.82,G:0.5,R:0.82}`. Status: refreshed 2026-05-19; decay trigger: benchmark harness CLI/env contract, benchmark JSON schema, broker images, service-container networking, or `spec/perf/` threshold suite introduction.

- [LM-004J|guard] Manual release-gate CI now executes the opt-in backpressure and Docker chaos specs against RabbitMQ 3.13.7 and LavinMQ 2.4.0. Backpressure uses service containers with `AMQP_BACKPRESSURE_LIVE=1`; chaos starts a named broker container so `spec/chaos_spec.cr` can pause/restart it through `AMQP_CHAOS_DOCKER_CONTAINER`. `SpecHelper.broker_reachable?` now requires a minimal AMQP open/close, not just TCP accept, to avoid RabbitMQ startup EOF races. Evidence: `.github/workflows/release-gates.yml`, `spec/spec_helper.cr`; local RabbitMQ and LavinMQ backpressure gates exit 0 with 7 examples each, and chaos gates exit 0 with 2 examples each. Trust `{F:0.82,G:0.54,R:0.82}`. Status: configured 2026-05-19; decay trigger: broker images, Docker availability on GitHub runners, service-container networking, chaos env contract, broker readiness behavior, or live spec gating changes.

- [LM-004K|guard] Manual release-gate CI now provisions a RabbitMQ 3.13.7 TLS broker from generated one-day test certificates and runs `spec/tls_spec.cr` through `AMQP_TLS_URL` / `AMQP_TLS_CA_CERT`. The job first verifies the listener with `openssl s_client` so the spec is not racing broker startup or certificate wiring. Evidence: `.github/workflows/release-gates.yml`; local generated-cert RabbitMQ TLS gate verifies with `openssl s_client` and exits 0 with 5 examples and no pending specs. Trust `{F:0.82,G:0.55,R:0.82}`. Status: configured 2026-05-19; decay trigger: RabbitMQ TLS config syntax, OpenSSL CLI behavior, generated-cert fixture, Docker mount semantics, GitHub runner Docker behavior, or TLS spec env contract changes.

- [LM-004L|guard] Manual release-gate CI now provisions a LavinMQ 2.4.0 TLS broker from generated one-day test certificates and runs `spec/tls_spec.cr` through `AMQP_TLS_URL` / `AMQP_TLS_CA_CERT`. The job starts `cloudamqp/lavinmq:2.4.0` with `--amqps-port`, `--cert`, and `--key`, then verifies the listener with `openssl s_client` before running the Crystal spec. Evidence: `.github/workflows/release-gates.yml`; local generated-cert LavinMQ TLS gate verifies with `openssl s_client` and exits 0 with 5 examples and no pending specs. Trust `{F:0.82,G:0.55,R:0.82}`. Status: configured 2026-05-19; decay trigger: LavinMQ TLS CLI flags, OpenSSL CLI behavior, generated-cert fixture, Docker mount semantics, GitHub runner Docker behavior, or TLS spec env contract changes.

- [LM-005|goal] Current working slice is "bring the public API and behavioral specs into alignment before expanding implementation". Evidence: user request to align API to docs and then quadrumvirate missing spec risks. Trust `{F:0.6,G:0.8,R:0.7}`. Status: active; decay trigger: user changes project priority.

- [LM-006|refute] The documented `select when msg = sub.receive` contract was false before this review: compiler reported `undefined method 'receive_select_action' for Amqp::Subscription`. Fixed by delegating select actions to the internal mailbox and adding `spec/api_surface_spec.cr` coverage. Trust `{F:1.0,G:0.8,R:1.0}`. Status: verified 2026-05-15; decay trigger: Subscription receive/select implementation changes.

- [LM-007|decision] Recovery during-window cutline: v0 new operations fail fast with `RecoveryInProgress`; the shard does not queue new caller work during recovery. Pre-disconnect unconfirmed publishes remain replay-owned by the confirm tracker. Evidence: `docs/12-recovery.md`, `docs/08-publisher-confirms.md`, `spec/recovery_spec.cr`. Trust `{F:0.9,G:0.8,R:0.9}`. Status: verified 2026-05-15; decay trigger: recovery wait/queue semantics are reintroduced.

- [LM-008|pattern] Publisher confirm negative-path coverage was the cheapest high-value spec slice and is now covered by `LM-014`, `LM-014B`, and `LM-014C`: nack, timeout, late ack, channel close while waiting, async close-no-value, unread outcome, and out-of-order ack all have focused guards. Trust `{F:0.9,G:0.8,R:0.9}`. Status: verified 2026-05-15; decay trigger: confirm settlement/wait/outcome logic changes.

- [LM-009|bugfix] Recovery fail-fast race: after `Connection#__force_disconnect_for_test`, the connection could be `Recovering` while an existing channel still looked `Open`, so `Channel#publish` wrote to a closed stream and raised raw `IO::Error` instead of `RecoveryInProgress`. Fixed by gating channel operations on `connection.state_recovering?`; verified by `crystal spec spec/recovery_spec.cr`. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: channel state/recovery gate changes.

- [LM-010|bugfix] `publish_confirm(timeout: 0.seconds)` previously registered/wrote a publish and then timed out with delivery tag 1, violating docs that require a synthetic timeout before any frames. Fixed by raising `PublishTimeoutError` with tag 0 before registration/write; verified by `spec/confirms_spec.cr` checking the queue remains empty. Trust `{F:0.9,G:0.8,R:0.9}`. Status: verified 2026-05-15; decay trigger: publish_confirm timeout path changes.

- [LM-011|bugfix] Docs referenced `PublishOutOfOrderError`, but the class and executable falsifier were missing. Added the error class and a deterministic confirm-tracker invariant spec for unknown single and multiple ack tags. Trust `{F:0.8,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: confirm settlement logic changes.

- [LM-012|decision] v0 URI query surface is deliberately narrow: recognized keys are `heartbeat`, `channel_max`, `frame_max`, `max_body_size`, `connect_timeout`, `tcp_nodelay`, `buffer_size`, `recovery`, `product`, and `information`; TLS policy keys and `auth_mechanism=EXTERNAL` are rejected as unknown instead of partially implemented. Evidence: `src/amqp/config.cr`, `spec/config_spec.cr`, `docs/04-uri-and-config.md`, `docs/11-tls.md`. Trust `{F:0.9,G:0.8,R:0.9}`. Status: refreshed 2026-05-19; decay trigger: URI query matrix or SASL/TLS implementation changes.

- [LM-013|decision] Public API docs now treat rich `ConnectionStats`/`ChannelStats`/`SubscriptionStats` and recovery callbacks as deferred, while v0 exposes `Amqp::Stats`, `Connection#heartbeat : Time::Span`, and `Connection#recovery_mode`. `spec/api_surface_spec.cr` also guards the top-level `Amqp` constants so visible implementation names stay intentional. Evidence: `docs/02-public-api.md`, `src/amqp/connection.cr`, `spec/api_surface_spec.cr`. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: public namespace, observability, or recovery callback API changes.

- [LM-014|bugfix] Publisher confirm nack and abort cleanup now have deterministic falsifiers: single nack, multiple=true nack range, and channel abort closing async outcome channels without a value. Evidence: `spec/confirms_spec.cr`; `crystal spec spec/confirms_spec.cr` passes. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: confirm tracker settlement or abort cleanup changes.

- [LM-014B|bugfix] Publisher confirm timeout and mid-wait close now have deterministic falsifiers: a timed-out publish remains tracked until a late ack cleans it up, and a blocked confirm waiter wakes with `ChannelClosedByCaller` when the channel aborts. Evidence: `spec/confirms_spec.cr`; `crystal spec spec/confirms_spec.cr` passes with 19 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: publish_confirm wait path changes.

- [LM-014C|guard] Async confirm receiver-drop behavior has a deterministic non-blocking guard: settling an unread async outcome channel completes and clears the confirm tracker instead of blocking the settlement path. Evidence: `spec/confirms_spec.cr`; `crystal spec spec/confirms_spec.cr --error-trace` exits 0 with 21 examples. Trust `{F:0.9,G:0.6,R:0.9}`. Status: verified 2026-05-15; decay trigger: confirm outcome channel capacity or settlement send semantics change.

- [LM-015|bugfix] `Subscription#receive`/`receive?` must not precheck `@mailbox.empty?` after close: Crystal channel `empty?` on a closed buffered channel can invalidate the drain expectation. Fixed by delegating directly to `receive?`; verified by `spec/subscription_spec.cr` drain tests. Trust `{F:0.9,G:0.6,R:0.9}`. Status: verified 2026-05-15; decay trigger: subscription mailbox/close implementation changes.

- [LM-015B|decision] `Subscription#spawn_loop` v0 is a simple spawned `each` loop; automatic reject-on-exception and termination callbacks are deferred. Caller-side cancel and broker-side queue-deletion cancel have focused specs. Evidence: `docs/02-public-api.md`, `docs/09-consumer.md`, `spec/subscription_spec.cr`. Trust `{F:0.8,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: spawn_loop semantics or consumer cancel handling changes.

- [LM-016|decision] Concrete performance numbers are roadmap targets, not v0 release guarantees, until a reproducible `spec/perf/` harness exists. Evidence: `docs/14-performance-contract.md` renamed to Performance Roadmap; `docs/16-falsifier-matrix.md` marks `T-PERF-*` as reserved. Trust `{F:0.9,G:0.8,R:0.9}`. Status: verified 2026-05-15; decay trigger: perf harness added or CI perf baseline introduced.

- [LM-017|decision] v0 error and observability docs describe the implemented reduced surface: typed exceptions without `CloseReason`/`ErrorContext`/`Error.recoverable?`, and one `Amqp::Stats` snapshot model instead of rich per-connection/channel/subscription stats. Evidence: `docs/03-error-model.md`, `docs/19-observability.md`, `docs/16-falsifier-matrix.md`; focused API/stats specs pass. Trust `{F:0.8,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: richer error or stats APIs are implemented.

- [LM-018|decision] Older architecture docs no longer contain normative references to deferred v0 public names such as `ConnectionStats#blocked?` and `Amqp::CloseReason`; remaining matches explicitly frame those names as deferred or absent from v0. Evidence: targeted `rg` over `docs TODO.md LANDMARKS.md`; patched `docs/05-wire-0-9-1/02-classes-methods.md` and `docs/18-amqp-1-0-forward-plan.md`. Trust `{F:0.8,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: docs add new public API claims without matching implementation.

- [LM-019|guard] Doc-link lint now guards falsifier drift: explicit `Falsifier: T-*` references must resolve through `docs/16-falsifier-matrix.md`, and the current unlinked `MUST`/`MUST NOT` section debt is pinned as a baseline so new unlinked normative sections fail specs. Evidence: `spec/docs_falsifier_link_spec.cr`; focused spec exits 0. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: docs section parser, falsifier ID syntax, or matrix structure changes.

- [LM-020|bugfix] URI/config negative coverage now guards the narrowed v0 surface: deferred TLS/SASL query keys are `UriError`, `amqp://` plus TLS context is `TlsConfigError`, unsupported schemes and missing hosts are `UriError`, and percent-encoded reserved userinfo decodes correctly. Evidence: `spec/config_spec.cr`; focused config/TLS specs pass. Trust `{F:0.9,G:0.8,R:0.9}`. Status: verified 2026-05-15; decay trigger: URI query or TLS config surface changes.

- [LM-021|bugfix] Full Crystal format check is now clean after formatting the known legacy offenders under `src/amqp/arguments.cr`, wire codec files, `src/amqp/stats.cr`, and wire specs. Evidence: `crystal tool format --check src spec` exits 0. Trust `{F:1.0,G:1.0,R:1.0}`. Status: verified 2026-05-15; decay trigger: new unformatted Crystal edits.

- [LM-022|verification] Live TLS handshake coverage is runnable locally with `AMQP_TLS_URL` plus `AMQP_TLS_CA_CERT`; the spec uses the supplied CA in a normal client context instead of disabling verification. Evidence: local `amqp-ng-rabbit-tls` listener on 5671, `openssl s_client` verification OK, and `spec/tls_spec.cr` exits 0 with no pending examples under those env vars. Trust `{F:0.9,G:0.6,R:0.8}`. Status: verified 2026-05-15; decay trigger: TLS spec env contract or RabbitMQ TLS config changes.

- [LM-023|finding] The slow-subscription hazard is per-channel first, not immediately connection-wide: the connection reader dispatches into a bounded per-channel frame inbox, while the channel handler blocks on the subscription mailbox. Docs and falsifier text now reflect this; an invasive live harness is gated by `AMQP_BACKPRESSURE_LIVE` until cleanup can be made safe. Evidence: `src/amqp/channel.cr`, `docs/09-consumer.md`, `spec/subscription_spec.cr`; default full spec passes with the harness pending. Trust `{F:0.8,G:0.7,R:0.8}`. Status: partially verified 2026-05-15; decay trigger: delivery routing, channel inbox capacity, or subscription mailbox implementation changes.

- [LM-023B|guard] `AMQP_BACKPRESSURE_LIVE=1` now runs a bounded live broker measurement: it fills one subscription mailbox without saturating the channel frame inbox and verifies an unrelated channel RPC on the same connection completes. The earlier harness hang was caused by a blocking/invalid test accessor (`@mailbox.size`); the spec now uses a test-only internal queue-depth accessor. Evidence: `spec/subscription_spec.cr`; opt-in focused spec exits 0 under a 20s timeout. Trust `{F:0.9,G:0.6,R:0.9}`. Status: verified 2026-05-15; decay trigger: Crystal `Channel` internals, subscription mailbox implementation, or routing topology changes.

- [LM-024|guard] Structural leak coverage now includes repeated connect/close clearing the connection channel registry and recovery topology records shrinking after consumer/topology removal. Async confirm outcome cleanup and subscription drain are covered by focused confirm/subscription specs. Evidence: `spec/connection_spec.cr`, `spec/recovery_spec.cr`, `spec/confirms_spec.cr`, `spec/subscription_spec.cr`; focused connection+recovery spec exits 0. Trust `{F:0.8,G:0.6,R:0.9}`. Status: verified 2026-05-15; decay trigger: channel registry, topology recording, confirm tracker, or subscription close changes.

- [LM-025|guard] Deterministic chaos coverage now includes broker channel-close mid-RPC and mandatory-return ordering. RabbitMQ passive-declare on a missing queue yields `ChannelClosedByBroker` 404, not `PreconditionFailedError`; mandatory return for one async publish does not contaminate the following acked publish. Evidence: `spec/channel_spec.cr`, `spec/confirms_spec.cr`; focused spec exits 0. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: channel close mapping or confirm return correlation changes.

- [LM-026|tooling] Grok ACP delegation remains usable for bounded review tasks through `/Users/sergey/.grok/bin/grok_review` and `grok_worker`; `--help` now passes through correctly, and a stdio smoke task returned `OK amqp-ng ACP smoke`. Evidence: wrapper help output and ACP smoke run. Trust `{F:0.9,G:0.5,R:0.9}`. Status: verified 2026-05-15; decay trigger: Grok CLI, auth, ACP protocol, or wrapper path changes.

- [LM-027|guard] Broker chaos coverage is split into default-safe deterministic specs and opt-in destructive Docker fixtures: channel close mid-RPC and mandatory return ordering run by default, while heartbeat-death and real broker restart require `AMQP_CHAOS_DOCKER_CONTAINER`. Evidence: `spec/channel_spec.cr`, `spec/confirms_spec.cr`, `spec/chaos_spec.cr`. Trust `{F:0.8,G:0.7,R:0.8}`. Status: verified harness 2026-05-15; decay trigger: chaos env contract, Docker broker topology, heartbeat detection, or recovery behavior changes.

- [LM-028|bugfix] Recovery session-loss handling is now idempotent across stale old-reader errors, and broker-forced `connection.close` reply-code 320/541 is recoverable while non-forced broker close remains terminal. Evidence: `spec/recovery_spec.cr` exits 0 with forced-close and non-forced-close classifier coverage; `AMQP_CHAOS_DOCKER_CONTAINER=amqp-ng-rabbit timeout 120 crystal spec spec/chaos_spec.cr --error-trace` exits 0 with real broker pause/restart coverage; full local opt-in suite exits 0 with 150 examples and 0 pending. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: connection state machine, broker close mapping, or recovery loop changes.

- [LM-029|bugfix] Pending confirm replay now remaps default-exchange publishes whose routing key was an old server-named queue. Without this, recovery recreated the queue under a fresh broker name, replayed the pending publish to the stale name, and RabbitMQ closed the channel with 404. Evidence: `spec/recovery_spec.cr` adds a server-named queue + consumer + pending confirm replay case and exits 0 with 8 examples; default full suite exits 0 with 150 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: queue rename recovery or pending confirm replay changes.

- [LM-030|guard] Caller close during the recovery window is final: the recovery pipeline does not re-open the connection after `Connection#close`, recovery success count stays zero, and further channel allocation raises on the closed connection. Evidence: `spec/recovery_spec.cr` exits 0 with 9 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: connection close or recovery state-machine changes.

- [LM-031|guard] Topology replay rejection fails closed instead of leaving a partially recovered connection available. A test-only unreplayable `amq.*` queue record makes RabbitMQ reject replay, and recovery closes with `RecoveryExhaustedError`, marks the channel closed, and increments failed recovery stats. Evidence: `spec/recovery_spec.cr` exits 0 with 10 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: topology replay failure handling changes.

- [LM-032|guard] The at-least-once duplicate caveat is executable: if an original publish is already in the broker queue and an equivalent pending confirm is replayed after recovery, the queue contains two copies and the replay outcome still resolves. Evidence: `spec/recovery_spec.cr` exits 0 with 11 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: pending confirm replay or reliability contract changes.

- [LM-033|guard] Explicit consumer tags are re-installed across recovery and post-recovery deliveries keep the same tag. Empty consumer tags are v0 client-generated before `basic.consume`, not broker-assigned; docs now match that race-avoidance design. Evidence: `spec/recovery_spec.cr` plus `spec/docs_falsifier_link_spec.cr` exit 0 with 14 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: consume tag generation or consumer replay changes.

- [LM-034|guard] Consumer replay rejection fails closed instead of leaving a partially recovered connection available. A test-only recorded consumer for a missing queue makes RabbitMQ reject `basic.consume`, and recovery closes with `RecoveryExhaustedError`, marks the channel closed, and increments failed recovery stats. Evidence: `spec/recovery_spec.cr` exits 0 with 13 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: consumer replay failure handling changes.

- [LM-035|verification] LavinMQ is now locally smoke-verified, not just documented: `cloudamqp/lavinmq:2.4.0` on `127.0.0.1:5673` passes the default suite with 150 examples / 4 live-gated pending examples, and passes opt-in backpressure plus Docker pause/restart chaos with only TLS pending. The previously documented `cloudamqp/lavinmq:2.0.3` image tag does not resolve. Trust `{F:0.9,G:0.5,R:0.9}`. Status: verified 2026-05-15; decay trigger: LavinMQ image/tag, broker behavior, or AMQP_URL-gated spec changes.

- [LM-036|decision] AMQP 1.0 now has a v1-only SDD contract rather than runtime wiring: `docs/22-amqp-1-0-sdd.md` defines codec, message, SASL/connection, link, API, recovery, and broker-matrix falsifier slices; `docs/16-falsifier-matrix.md` reserves `T-AMQP10-*` as v1 rows; `docs/17-mvp-cutline.md` still forbids AMQP 1.0 in v0. Trust `{F:0.8,G:0.7,R:0.8}`. Status: docs planned 2026-05-15; decay trigger: AMQP 1.0 code or protocol-selection API is added.

- [LM-037|release] v0.1.0 is now the pragmatic downstream integration release: `shard.yml` version is 0.1.0, `Amqp::VERSION` is 0.1.0, `CHANGELOG.md` records current evidence/deferred surfaces, and a private downstream service compiles against the local `amqp` path dependency. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified locally 2026-05-15; decay trigger: shard version, dependency wiring, or downstream MQ API changes.

- [LM-038|performance] Local RabbitMQ microbench parity is close but not complete after hot-path cleanup. `amqp-ng` now writes empty-property publish frames directly, avoids a copy for single-frame inbound bodies, and avoids holding the confirm mutex across non-recovery socket writes while preserving confirm seq/write ordering. On Crystal 1.20.1 release, 20k publish / 3k confirm medians were ~65.5k msg/s and ~3.07k msg/s for `amqp-ng` versus ~67.6k msg/s and ~3.29k msg/s for `amqp-client.cr`; old `amqp-client.cr` does not compile on the local Crystal 1.20.0-dev compiler. A later checked-in `tools/perf_publish.cr` run on the local dev compiler reported ~62.0k msg/s single publish, ~68.5k msg/s batch publish, ~3.07k msg/s sync confirms, and ~69.7k msg/s batch confirm+wait with batch size 100. Trust `{F:0.8,G:0.4,R:0.8}`. Status: verified locally 2026-05-15; decay trigger: benchmark harness, compiler, broker, publish path, or confirm tracker changes.

- [LM-039|parity] Practical v0 feature parity with the Crystal `amqp-client.cr` migration surface is now close enough for the local downstream release: lifecycle, topology, publish/confirm, get/consume/ack, recovery-adjacent operations, queue/exchange wrappers, callbacks, broker blocked/unblocked, `channel.flow`, `basic.recover`, transactions, work-pool `basic_consume`, IO publish overloads, NamedTuple `args` sugar, and broker no-wait frames for accepted compatibility overloads are covered. WebSocket is treated as a future transport research item, not a Crystal-shard parity blocker. Trust `{F:0.9,G:0.68,R:0.9}`. Status: refreshed 2026-05-18; decay trigger: public API additions, amqp-client.cr comparison refresh, or a migration site requiring stricter no-wait behavior outside the accepted overload set.

- [LM-040|performance] Cogni/LTP-style fast paths must carry exactness witnesses. Direct `basic.publish` and empty content-header writers now have byte-equivalence specs against the generic frame encoder, and `Channel#publish_batch` has live broker ordering plus confirm-sequence coverage. Evidence: `spec/wire/basic_methods_spec.cr`, `spec/wire/content_header_spec.cr`, `spec/channel_spec.cr`, `spec/confirms_spec.cr`; focused wire/channel/confirm spec exits 0 with 64 examples. Trust `{F:0.9,G:0.6,R:0.9}`. Status: verified 2026-05-15; decay trigger: publish encoder, content-header encoder, batch API, or confirm tracker changes.

- [LM-041|performance] Pending confirm replay retention is now mode-aware: `Recovery::None` entries keep no replay `Message`, while `Recovery::Full` entries retain replay payloads for reconnect republish. `publish_batch` is also documented in public API and publisher-confirm docs. Evidence: `src/amqp/channel.cr`, `docs/02-public-api.md`, `docs/08-publisher-confirms.md`; focused API/confirm/recovery spec exits 0 with 41 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: confirm registration, recovery mode semantics, or public publish API changes.

- [LM-042|performance] `wait_for_confirms` no longer scans `@unconfirmed.min` on each progress check. The confirm tracker maintains `@lowest_unconfirmed` and refreshes it on single settlement, multiple settlement, discard, and clear paths. Evidence: `src/amqp/channel.cr`; focused confirm/recovery spec exits 0 with 37 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-15; decay trigger: confirm tracker data structure or settlement logic changes.

- [LM-043|performance] The checked-in publish harness now separates synthetic empty-publish frame encoding from live broker publish throughput and includes multi-channel probes. A short local run showed ~7.3M ops/s for direct empty-publish frame encoding but only ~65k msg/s live publish across 1/2/4 channels on one connection, so the next likely bottleneck is the shared connection write/socket/broker path rather than the direct codec fast path. Evidence: `tools/perf_publish.cr`; short attribution run on local RabbitMQ. Trust `{F:0.8,G:0.4,R:0.8}`. Status: verified directionally 2026-05-16; decay trigger: harness, compiler, broker, connection write path, or runtime threading changes.

- [LM-044|performance] The publish harness now has multi-connection probes. A short local run on one shared named queue was noisy but showed multi-connection publish can exceed the single-connection lane, while multi-channel-on-one-connection did not reliably scale. This is directional evidence that the single connection write/socket path is a real ceiling and that any writer-fiber redesign should be compared against multi-connection sharding. Evidence: `tools/perf_publish.cr`; short multi-connection run on local RabbitMQ. Trust `{F:0.75,G:0.35,R:0.75}`. Status: directionally verified 2026-05-16; decay trigger: harness queue topology, compiler, broker, connection count, or runtime threading changes.

- [LM-045|performance] Separate-queue multi-connection publish probes are now in the checked-in harness, and benchmark application logs are suppressed so the tool emits clean JSON. A short local run showed one-connection multi-channel around ~71-74k msg/s, shared-queue multi-connection around ~104-155k msg/s, and separate-queue 2/4 connection publish around ~216-225k msg/s. This suggests both client connection serialization and broker shared-queue contention matter; queue-sharded multi-connection publishing is currently the strongest measured throughput lane. Evidence: `tools/perf_publish.cr`; short separate-queue run on local RabbitMQ. Trust `{F:0.78,G:0.35,R:0.78}`. Status: directionally verified 2026-05-16; decay trigger: harness queue topology, compiler, broker queue implementation, connection count, or runtime threading changes.

- [LM-046|performance] `amqp-client.cr` supports the same broad benchmark shapes with ordinary multiple channels and multiple connections, but the local old shard does not compile on the dev Crystal because `amq-protocol` reaches into the old `IO::Memory @writeable` ivar. On release Crystal 1.20.1, a scratch matching harness reported ~80k msg/s single publish, ~82-88k msg/s multi-channel same-connection publish, ~254-476k msg/s shared-queue multi-connection publish, ~560-658k msg/s separate-queue 2/4 connection publish, and ~2.68k msg/s sync confirms. These are directional and need paired quiet reruns before promotion because multi-connection samples were noisy. Evidence: `.tmp/bench/amqp_client_modes_bench.cr`, `/opt/homebrew/bin/crystal 1.20.1`, local RabbitMQ. Trust `{F:0.78,G:0.35,R:0.78}`. Status: directionally verified 2026-05-16; decay trigger: old shard version, compiler, broker, queue topology, or harness changes.

- [LM-047|performance] Socket policy was a major publish-throughput limiter. `amqp-ng` now exposes `tcp_nodelay` and `buffer_size` URI/keyword config, defaulting to `tcp_nodelay=false` and `buffer_size=16384` like the old client. On release Crystal 1.20.1, a short local run improved `amqp-ng` to ~76.9k msg/s single publish, ~615k/~511k msg/s separate-queue 2/4 connection publish, and ~2.89k msg/s sync confirms. A contrast run with `tcp_nodelay=true&buffer_size=16384` dropped separate-queue 2/4 connection publish back to ~211k/~206k msg/s. Evidence: `src/amqp/config.cr`, `src/amqp/connection.cr`, `spec/config_spec.cr`, `docs/04-uri-and-config.md`, local RabbitMQ release runs. Trust `{F:0.85,G:0.45,R:0.82}`. Status: directionally verified 2026-05-16; decay trigger: socket defaults, benchmark host, broker, compiler, or runtime IO changes.

- [LM-048|performance] Non-confirm fire-and-forget publish no longer takes the per-channel continuation lock; it relies on the connection write mutex to keep each method/header/body sequence atomic. This matches the publisher-confirm doc contract and is guarded by a live concurrent same-channel publish test. On paired release Crystal 1.20.1 runs with 9000 publish / 900 confirm / 3 samples, `amqp-ng` reported ~83.6k msg/s single publish, ~85.1k msg/s batch publish, ~705k/~625k msg/s separate-queue 2/4 connection publish, and ~2.48k msg/s sync confirms; `amqp-client.cr` reported ~83.0k msg/s single publish, ~682k/~621k msg/s separate-queue 2/4 connection publish, and ~2.52k msg/s sync confirms. Evidence: `src/amqp/channel.cr`, `spec/channel_spec.cr`, `docs/02-public-api.md`, `docs/07-channel-lifecycle.md`, paired local release benchmark runs. Trust `{F:0.88,G:0.45,R:0.84}`. Status: directionally verified 2026-05-16; decay trigger: publish locking, confirm-mode transition semantics, benchmark host, broker, compiler, or runtime IO changes.

- [LM-049|parity] The practical `basic_*` alias slice is now implemented for common `amqp-client.cr` migration paths: publish, callback publish, publish_confirm, get, consume, cancel, ack, reject, nack, and qos. The aliases are thin wrappers over the amqp-ng API, with callback publish auto-enabling confirm mode and reporting ack as `true`. Evidence: `src/amqp/channel.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused API/channel/confirm spec exits 0 with 41 examples. Trust `{F:0.88,G:0.65,R:0.9}`. Status: verified 2026-05-16; decay trigger: public channel method signatures, confirm alias behavior, or amqp-client.cr compatibility target changes.

- [LM-050|parity] Queue/exchange wrapper objects are now implemented for common `amqp-client.cr` migration paths. `Amqp::Queue` supports bind/unbind, publish, publish_confirm, get, subscribe/unsubscribe, purge/delete, and passive count checks; `Amqp::Exchange` supports bind/unbind, publish, publish_confirm, and delete; `Channel` exposes wrapper constructors for default/direct/topic/fanout/headers exchanges and queues. Evidence: `src/amqp/queue.cr`, `src/amqp/exchange.cr`, `src/amqp/channel.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused API/channel spec exits 0 with 19 examples. Trust `{F:0.88,G:0.65,R:0.9}`. Status: verified 2026-05-16; decay trigger: wrapper method signatures, old-client compatibility target, queue/exchange channel APIs, or broker exchange defaults change.

- [LM-051|parity] `Channel#on_return` now supports amqp-client.cr-style mandatory publish return callbacks. `basic.return` still records `ReturnReason` for confirm correlation, then emits `ReturnedMessage` to the callback on a spawned fiber so user code cannot block the channel handler. Evidence: `src/amqp/channel.cr`, `src/amqp/message.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`, existing `spec/confirms_spec.cr`; focused API/channel/confirm spec exits 0 with 45 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-16; decay trigger: basic.return assembly, confirm correlation, callback API, or returned-message payload changes.

- [LM-052|parity] Synchronous `basic.recover` is now implemented as `Channel#basic_recover`, including wire codec support for `basic.recover` and `basic.recover-ok`. A live broker smoke verifies an unacked `basic.get` delivery can be requeued and redelivered; `basic.recover-async` remains deferred. Evidence: `src/amqp/wire/amqp_zero_nine_one/basic_methods.cr`, `src/amqp/channel.cr`, `spec/wire/basic_methods_spec.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused wire/API/channel spec exits 0 with 26 examples. Trust `{F:0.9,G:0.75,R:0.9}`. Status: verified 2026-05-16; decay trigger: basic method codec, get/ack delivery semantics, or broker recover behavior changes.

- [LM-053|parity] `connection.blocked` / `connection.unblocked` observability is now public via `Connection#blocked?`, `Connection#on_blocked`, and `Connection#on_unblocked`. Synthetic connection frames verify blocked reason parsing, flag transitions, and callback dispatch on spawned fibers. Evidence: `src/amqp/wire/amqp_zero_nine_one/connection_methods.cr`, `src/amqp/connection.cr`, `spec/api_surface_spec.cr`, `spec/connection_spec.cr`; focused API/connection spec exits 0 with 14 examples. Trust `{F:0.88,G:0.65,R:0.9}`. Status: verified 2026-05-16; decay trigger: connection async method handling, callback API, or broker blocked extension changes.

- [LM-054|parity] Runtime `channel.flow` handling is now implemented. Inbound `channel.flow(active=false)` gates publish paths; `active=true` wakes blocked publishers, and the handler replies with `channel.flow-ok`. A focused live test uses the internal flow gate to verify a publish does not complete while inactive and resumes after reactivation. Evidence: `src/amqp/channel.cr`, `src/amqp/wire/amqp_zero_nine_one/channel_methods.cr`, `spec/channel_spec.cr`; focused channel spec exits 0 with 16 examples. Trust `{F:0.86,G:0.6,R:0.88}`. Status: verified 2026-05-16; decay trigger: publish path locking, channel flow handling, or broker flow semantics change.

- [LM-055|parity] Broker-native AMQP transactions are now exposed as `Channel#tx_select`, `#tx_commit`, `#tx_rollback`, and `#transaction`, with tx wire codec coverage. A live broker smoke verifies rollback discards a transactional publish and commit makes it visible. Recovery does not preserve in-flight transactions; docs explicitly route recoverable publish flows to publisher confirms. Evidence: `src/amqp/wire/amqp_zero_nine_one/tx_methods.cr`, `src/amqp/channel.cr`, `spec/wire/tx_methods_spec.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused tx/API/channel spec exits 0 with 25 examples. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-16; decay trigger: tx method codec, channel transaction API, broker tx behavior, or recovery transaction semantics change.

- [LM-056|parity] Channel callback/flow compatibility now covers old-client `on_cancel`, `on_close`, and client-initiated `flow(active)`. Broker `basic.cancel` also replies with `basic.cancel-ok` when `no-wait=false`, matching the consumer spec instead of silently dropping the acknowledgement. Evidence: `src/amqp/channel.cr`, `src/amqp/wire/amqp_zero_nine_one/basic_methods.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`, `spec/wire/basic_methods_spec.cr`; focused API/channel/basic-wire spec exits 0 with 29 examples. Trust `{F:0.9,G:0.65,R:0.9}`. Status: verified 2026-05-16; decay trigger: broker cancel handling, channel close handling, flow codec, or callback API changes.

- [LM-057|parity] IO publish compatibility now exists for old-client migration paths that pass an `IO` plus explicit byte size. The v0 implementation reads that slice into the existing in-memory publish body model, so this is API compatibility rather than a zero-copy streaming publish path. Evidence: `src/amqp/channel.cr`, `src/amqp/queue.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused API/channel spec exits 0 with 26 examples. Trust `{F:0.85,G:0.6,R:0.85}`. Status: verified 2026-05-16; decay trigger: publish body model, queue wrapper API, or streaming publish implementation changes.

- [LM-058|verification] Post-parity full regression gate is green after callback/flow/cancel and IO publish additions. Evidence: `crystal spec --error-trace` exits 0 with 174 examples, 0 failures, 0 errors, 4 pending; `crystal tool format --check src spec tools/perf_publish.cr`, `crystal build tools/perf_publish.cr --no-codegen --error-trace`, `git diff --check`, and a private downstream service compile smoke exit 0. Trust `{F:0.95,G:0.8,R:0.95}`. Status: verified 2026-05-16; decay trigger: any source/spec/docs edit after this point.

- [LM-059|performance] The public `publish_batch(Array(Bytes))` overload now has a direct non-confirm path instead of first building an intermediate `Array(Message)`, with an empty-properties branch that avoids per-message property checks on the default hot path. Prebuilt-message unconfirmed batches also pre-detect all-empty properties outside the connection write lock, and all batch publish paths reuse one byte-equivalent `basic.publish` method frame per batch route. Confirm-mode batches intentionally keep the existing message path to preserve sequence/recovery semantics. The checked-in harness emits a separate `publish_batch_bytes` lane. Live RabbitMQ and LavinMQ runs confirm behavior, but throughput versus callers that already prebuild `Array(Message)` batches is broker/noise dependent: a post-route-frame-reuse 7-sample batch-focused release run reported RabbitMQ medians ~79.3k single publish, ~87.5k prebuilt-message batch, ~85.4k bytes batch, ~2.96k sync confirms, and ~74.1k batch confirm+wait; LavinMQ medians were ~754.0k single publish, ~736.9k prebuilt-message batch, ~845.2k bytes batch, ~4.29k sync confirms, and ~53.7k batch confirm+wait. A full 3-sample cross-broker run also showed ~795.5k RabbitMQ and ~777.2k LavinMQ for 4-connection separate-queue publish before route-frame reuse. Evidence: `src/amqp/channel.cr`, `src/amqp/wire/amqp_zero_nine_one/basic_methods.cr`, `spec/wire/basic_methods_spec.cr`, `tools/perf_publish.cr`, `.tmp/bench/*routeframe_batchfocus.json`, `.tmp/bench/*post2.json`; live default suites for both brokers exit 0 with 175 examples, 0 failures, 0 errors, 4 pending; `/opt/homebrew/bin/crystal tool format --check src spec tools/perf_publish.cr`, `/opt/homebrew/bin/crystal build tools/perf_publish.cr --release --no-codegen --error-trace`, and `git diff --check` exit 0. Trust `{F:0.9,G:0.5,R:0.88}`. Status: live-verified directionally 2026-05-17; decay trigger: bytes batch API, confirm-mode batch path, publish method-frame writer, benchmark harness, broker version, compiler, or runtime IO changes.

- [LM-060|performance] Sync `publish_confirm` no longer allocates a per-publish outcome channel and no longer keeps a separate sync-waiter set. The pending confirm record carries the sync-waiter bit, settlement stores completed sync outcomes only for still-active waiters, and timeout abandons only the waiter result slot while leaving the pending confirm cleanup path intact. `publish_confirm` also no longer takes the method-continuation operation lock; confirms are async broker methods correlated by delivery tag, and a live spec covers concurrent `publish_confirm` on one channel. On focused paired release runs (`30000` publishes, `3000` sync confirms, `7` samples, one channel/connection), RabbitMQ reported `amqp-ng` ~77.2k msg/s single publish, ~2.93k msg/s sync confirms, and ~79.1k msg/s batch confirm+wait versus `amqp-client.cr` ~79.5k msg/s single publish and ~2.54k msg/s sync confirms; LavinMQ reported `amqp-ng` ~806.3k msg/s single publish, ~4.38k msg/s sync confirms, and ~55.5k msg/s batch confirm+wait versus `amqp-client.cr` ~781.2k msg/s single publish and ~4.45k msg/s sync confirms. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`, `docs/07-channel-lifecycle.md`, `docs/08-publisher-confirms.md`, `.tmp/bench/amqp_ng_confirmfast5_rabbitmq_focus_seq_20260517.json`, `.tmp/bench/amqp_client_confirmfast5_rabbitmq_focus_seq_20260517.json`, `.tmp/bench/amqp_ng_confirmfast5_lavinmq_focus_seq_20260517.json`, `.tmp/bench/amqp_client_confirmfast5_lavinmq_focus_seq_20260517.json`; focused live confirm spec exits 0 with 27 examples. Trust `{F:0.9,G:0.45,R:0.86}`. Status: live-verified directionally 2026-05-17; decay trigger: confirm waiter representation, timeout cleanup, publish locking, benchmark host, broker, compiler, or runtime scheduler changes.

- [LM-061|performance] The publish harness now has LTP-style stage attribution for `basic.ack` frame parsing. A short RabbitMQ probe (`3000` publish, `300` confirm, `200000` synthetic stage iterations, `3` samples) reported ~10.7M ops/s for the current generic `Frame.read` + `IO::Memory` + `Ack.read` route and ~563M ops/s for a direct zero-allocation `basic.ack` parser, while live sync confirms were only ~2.62k msg/s. This makes direct ack parsing a proven local spike but refutes it as the next dominant sync-confirm bottleneck for this workload; the next higher-probability moves are transport-level/windowed confirms or direct measurement of write/flush/ack-wait time. Evidence: `tools/perf_publish.cr`, `.tmp/bench/amqp_ng_ltp_stage_probe_20260517.json`; `/opt/homebrew/bin/crystal build tools/perf_publish.cr --release --no-codegen --error-trace` exits 0. Trust `{F:0.86,G:0.4,R:0.82}`. Status: directionally verified 2026-05-17; decay trigger: frame parser implementation, broker workload, confirm path, harness shape, compiler, or runtime scheduler changes.

- [LM-062|performance] Confirm-window sweeping makes the LTP Ladder move explicit. The harness now accepts `AMQP_BENCH_CONFIRM_WINDOWS` and emits `confirm_window_<n>` lanes. A short RabbitMQ run (`1000` confirms, windows `1,5,10,50,100,500`, `3` samples) reported ~2.80k msg/s sync confirms, ~2.87k window 1, ~11.6k window 5, ~18.7k window 10, ~43.2k window 50, ~47.6k window 100, ~72.5k window 500, and ~70.8k batch confirm+wait. `Channel#publish_confirm_batch` exposes that windowed corridor directly as a public helper over `publish_batch` + `wait_for_confirms`; it now writes `Array(Message)` windows by index instead of allocating a sliced array per window, and benchmark lanes exercise the public helper. This verifies that the next large practical win is an ergonomic/windowed confirm surface or prepared batching rather than more local ack parsing. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`, `tools/perf_publish.cr`, `.tmp/bench/amqp_ng_ltp_confirm_windows_api_20260517.json`, `.tmp/bench/amqp_ng_confirm_range_helper_20260517.json`; live RabbitMQ API+confirm spec exits 0 with 36 examples; release no-codegen build exits 0; post-range-helper short release harness reports ~2.70k sync confirms, ~44.7k window 100, ~60.9k window 500, and ~70.4k batch confirm+wait. Trust `{F:0.88,G:0.5,R:0.86}`. Status: live-verified directionally 2026-05-17; decay trigger: confirm batch implementation, public API surface, broker workload, socket policy, benchmark shape, compiler, or runtime scheduler changes.

- [LM-063|performance] Current paired release runs show `amqp-ng` is ahead of `amqp-client.cr` on several common publish lanes but not universally. The write hot path no longer takes a separate heartbeat timestamp mutex, and confirm batch/window paths pre-register pending confirms under one confirm-mutex acquisition per batch/window. With `15000` publishes, `1500` confirms, and `5` samples, RabbitMQ reports `amqp-ng` ~82.3k single publish (+13.5%), ~77.0k 4-channel same-connection publish (+14.8%), ~2.68k sync confirms (+6.4%), and ~65.0k window-500 confirms. LavinMQ reports `amqp-ng` ~744.6k single publish (-3.5%), ~822.4k 2-channel same-connection publish (+13.8%), ~694.0k 4-connection separate-queue publish (+2.2%), ~3.46k sync confirms (+2.7%), and ~33.6k batch confirm+wait. Remaining weak spots are LavinMQ single publish and noisy RabbitMQ multi-connection shared/separate queue lanes. Evidence: `src/amqp/connection.cr`, `src/amqp/channel.cr`, `.tmp/bench/current_ng_rabbitmq_batchreg_20260517.json`, `.tmp/bench/current_client_rabbitmq_20260517.json`, `.tmp/bench/current_ng_lavinmq_batchreg_20260517.json`, `.tmp/bench/current_client_lavinmq_20260517.json`; focused LavinMQ confirm/connection/heartbeat spec exits 0 with 39 examples; post-hot-path full default suites pass on RabbitMQ 3.13.x and LavinMQ 2.4.0 with 180 examples, 0 failures, 0 errors, 4 pending. Trust `{F:0.88,G:0.5,R:0.86}`. Status: directionally verified 2026-05-17; decay trigger: write path, confirm registration, benchmark shape, broker version, compiler, or runtime scheduler changes.

- [LM-064|performance] A LavinMQ-shaped `amqp-client` facade now compiles real Shovel source/destination files and the full `src/lavinmqperf.cr` target on Crystal 1.19.1 with `-Dpreview_mt -Dexecution_context`. The facade adds `AMQP::Client` wrapper classes, JSON-capable `Arguments`, minimal `AMQ::Protocol::Properties`, `body_io`, NamedTuple queue-declare shape, old-client error aliases, IO publish overloads, raw `AMQP::Client::Frame::Basic::Consume`, `Connection#write`, `Client#host=`, `Queue#delete`, and `queue_declare(no_wait:)`. Against a local LavinMQ 2.4.0 broker, focused live specs pass with 57 examples and the real `src/lavinmq/shovel/amqp_source.cr` / `amqp_destination.cr` compile under `CRYSTAL_PATH=/Users/sergey/Projects/Crystal/amqp-ng/src:lib:...`. Crystal 1.20.1 paired release probe (`1` publisher, `1` consumer, 256-byte bodies, 2,000,000 messages, `-Dpreview_mt -Dexecution_context`) reports original `amqp-client.cr` ~398.4k msg/s and amqp-ng shim ~497.6k msg/s, but Crystal 1.19.1 with the same shape reports practical parity: original ~331.7k and shim ~331.9k msg/s. Crystal 1.19.1 confirm callback mode (`--confirm 500`, 500,000 messages) also reports parity: original ~165.9k and shim ~165.9k msg/s. Direct inline callback settlement is refuted because it hung under `Fiber::ExecutionContext::Parallel`; a custom batched dispatcher is also refuted because it hung the live confirm spec. Trust `{F:0.9,G:0.52,R:0.88}`. Status: directionally verified 2026-05-18; decay trigger: facade API, confirm callback transport, LavinMQ perf runner, Crystal execution-context flags, broker version, or scheduler behavior changes.

- [LM-065|performance] The first higher-level LTP/WBA move for confirm callbacks is `Callback Isolation Collapse`: treat a slow/full confirm-callback dispatch queue as the window, broker ack settlement through the channel handler as the corridor, and `(handler_blocking_risk, callback_fiber_count, pending_confirms, latency)` as the lexicographic potential. `enqueue_confirm_callback` now performs a nonblocking send to the normal per-channel dispatcher and spawns an overflow callback fiber only when the queue cannot accept immediately. This removes the channel-handler head-of-line block in the pathological slow-callback/full-queue regime while preserving the zero-extra-fiber normal path. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`; live LavinMQ 2.4.0 confirm spec exits 0 with 31 examples, 0 failures, 0 errors, 0 pending, including a filled callback-dispatch queue falsifier. Trust `{F:0.9,G:0.6,R:0.9}`. Status: live-verified 2026-05-18; decay trigger: confirm callback dispatcher, channel handler settlement path, callback queue capacity, or scheduler behavior changes.

- [LM-066|performance] The second higher-level LTP/WBA move is `Single Publish Corridor Fusion`: treat repeated single-publish route method-frame writes under the connection write mutex as the window, the publish method/header/body sequence as the corridor, and `(write_lock_work, frame_write_calls, allocations_on_unique_routes, latency)` as the lexicographic potential. Single publish now uses a delayed one-entry method-frame cache: first-seen routes still direct-write, while repeated routes promote to the reusable method-frame corridor already used by batch publish. This reduces write-lock work for the dominant repeated-route workload without adding cached-frame allocation to unique-route workloads. Evidence: `src/amqp/channel.cr`; focused wire/channel/confirm specs, release no-codegen perf build, format check, and diff check exit 0; a short LavinMQ 2.4.0 release probe in `.tmp/bench/current_ng_lavinmq_methodframe_cache_20260518.json` reports `publish_single` median ~765.2k msg/s, `publish_batch_bytes` ~862.5k, `publish_multi_channel_2` ~835.1k, and `publish_multi_connection_2` ~819.3k. Trust `{F:0.86,G:0.5,R:0.82}`. Status: directionally verified 2026-05-18; decay trigger: publish method-frame writer/cache, benchmark shape, broker version, compiler, or runtime scheduler changes.

- [LM-067|performance] The third higher-level LTP/WBA move is `Confirm Ack Orbit Compression`: treat exact-size `basic.ack`/`basic.nack` broker method frames as the window, confirm settlement through the channel handler as the corridor, and `(handler_parse_work, transient_io_objects, pending_confirms, live_confirm_latency)` as the lexicographic potential. The channel handler now parses exact 13-byte ack/nack payloads directly from the frame slice before allocating `IO::Memory`; non-exact payloads keep the existing generic decoder path. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`; live LavinMQ 2.4.0 confirm spec exits 0 with 32 examples, 0 failures, 0 errors, 0 pending; `.tmp/bench/current_ng_lavinmq_direct_ack_20260518.json` reports local stage medians `parse_basic_ack_frame_direct` ~613.3M ops/s and `parse_basic_ack_frame_generic` ~13.8M ops/s. Live confirm throughput did not show a clean global win in that probe, so this is verified as handler CPU reduction rather than an end-to-end bottleneck removal. Trust `{F:0.9,G:0.45,R:0.84}`. Status: live-verified directionally 2026-05-18; decay trigger: method-frame handler, ack/nack payload shape, confirm settlement, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-068|performance] The fourth higher-level LTP/WBA move is `Bytes Confirm Corridor`: treat public `publish_confirm_batch(Array(Bytes))` and confirm-mode `publish_batch(Array(Bytes))` as the window, bytes body -> pending confirm registration -> publish frames -> confirm wait as the corridor, and `(message_wrapper_allocations, replay_payload_correctness, write_lock_work, live_confirm_latency)` as the lexicographic potential. Confirm-mode bytes batch publishing now registers confirms and writes frames directly from `Array(Bytes)` instead of first building a full `Array(Message)`. `Recovery::Full` still builds replay messages because it must retain payloads; `Recovery::None` avoids wrapper allocation. `tools/perf_publish.cr` now emits `confirm_batch_wait_bytes` and `confirm_window_bytes_<n>` lanes. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`, `tools/perf_publish.cr`; live LavinMQ 2.4.0 confirm spec exits 0 with 33 examples, 0 failures, 0 errors, 0 pending; `.tmp/bench/current_ng_lavinmq_bytes_confirm_20260518.json` reports `confirm_batch_wait_bytes` ~30.2k msg/s vs message batch ~28.2k, and `confirm_window_bytes_500` ~15.4k vs message window ~14.2k. Trust `{F:0.88,G:0.5,R:0.84}`. Status: live-verified directionally 2026-05-18; decay trigger: bytes publish API, recovery replay retention, confirm batch registration, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-069|performance] The fifth higher-level LTP/WBA move is `Default Confirm Window Policy`: treat default `publish_confirm_batch` barrier size as the window, user call without explicit tuning -> windowed publish -> broker confirm wait as the corridor, and `(default_roundtrips, outstanding_confirm_memory, timeout_risk, live_confirm_latency)` as the lexicographic potential. The default window is now 500 instead of 100 for both `Array(Message)` and `Array(Bytes)`, while explicit positive `window_size` remains caller-controlled. `tools/perf_publish.cr` now emits `confirm_window_default` and `confirm_window_bytes_default` lanes. Evidence: `src/amqp/channel.cr`, `docs/02-public-api.md`, `docs/08-publisher-confirms.md`, `tools/perf_publish.cr`; focused API/docs/confirm specs and release no-codegen perf build pass; `.tmp/bench/current_ng_lavinmq_window_default_20260518.json` reports default lanes around ~10.6-10.7k msg/s, explicit 100 lanes around ~3.4k, and explicit 500 lanes around ~10.7-15.0k on a short noisy LavinMQ 2.4.0 probe. Trust `{F:0.84,G:0.5,R:0.82}`. Status: directionally verified 2026-05-18; decay trigger: confirm window default, benchmark shape, broker version, compiler, scheduler behavior, or memory-pressure requirements change.

- [LM-070|performance] The sixth higher-level LTP/WBA move is `Empty Header Corridor Fusion`: treat repeated empty content-header frames for same-size bodies as the window, the publish method/header/body sequence under the connection write mutex as the corridor, and `(write_lock_work, header_frame_write_calls, allocations_on_unique_body_sizes, live_publish_latency)` as the lexicographic potential. `ContentHeader.empty_frame` now builds a reusable empty header frame, and the channel uses a delayed one-entry cache for repeated body sizes. First-seen body sizes still direct-write, so unique-size workloads do not allocate cached header frames. Evidence: `src/amqp/channel.cr`, `src/amqp/wire/amqp_zero_nine_one/content_header.cr`, `spec/wire/content_header_spec.cr`; focused wire/channel/confirm specs and release no-codegen perf build pass; `.tmp/bench/current_ng_lavinmq_header_cache_20260518.json` reports `publish_single` ~831.0k msg/s and `publish_batch_bytes` ~910.5k on a short noisy LavinMQ 2.4.0 probe. Trust `{F:0.86,G:0.5,R:0.82}`. Status: directionally verified 2026-05-18; decay trigger: content-header writer/cache, body-size distribution, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-071|performance] The seventh higher-level LTP/WBA move is `Batch Stats Corridor Collapse`: treat per-message `Stats#incr_published` calls inside confirm batch write-lock sections as the window, confirm batch registration -> frame write -> post-write stats as the corridor, and `(write_lock_work, atomic_counter_ops_under_lock, stats_correctness, live_batch_latency)` as the lexicographic potential. Confirm batch and confirm-window paths now increment published stats once per successful batch/window after the connection write lock is released, instead of once per message under `connection.with_write`. Evidence: `src/amqp/channel.cr`; live LavinMQ 2.4.0 stats+confirm spec exits 0 with 37 examples, 0 failures, 0 errors, 0 pending; `.tmp/bench/current_ng_lavinmq_stats_batch_20260518.json` shows batch/window lanes remained healthy but did not prove a clean end-to-end throughput win. Trust `{F:0.9,G:0.5,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: stats counters, batch publish registration, write-lock scope, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-072|performance] The eighth higher-level LTP/WBA move is `Body Prefix Corridor Fusion`: treat repeated body-frame prefixes for same-size publish chunks as the window, the publish method/header/body sequence under the connection write mutex as the corridor, and `(write_lock_work, body_prefix_write_calls, allocations_on_unique_chunk_sizes, live_publish_latency)` as the lexicographic potential. `Frame.prefix` now builds reusable frame prefixes with a byte-equivalence spec, and channel publish paths use a delayed one-entry cache for repeated body chunk sizes. First-seen chunk sizes still direct-write, so unique-size workloads do not allocate cached prefixes. Evidence: `src/amqp/channel.cr`, `src/amqp/wire/frame.cr`, `spec/wire/frame_spec.cr`; focused wire/channel/confirm specs, full default spec, release no-codegen perf build, format check, and live LavinMQ channel/confirm/stats specs pass; `.tmp/bench/current_ng_lavinmq_body_prefix_20260518.json` reports `publish_single` ~715.2k msg/s and `publish_batch_bytes` ~852.7k on a short noisy LavinMQ 2.4.0 probe. This is verified as write-lock work reduction, not as a clean end-to-end throughput win. Trust `{F:0.88,G:0.5,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: frame prefix writer/cache, body chunk distribution, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-073|performance] The ninth higher-level LTP/WBA move is `Empty Header Decode Collapse`: treat incoming empty content-header payloads for default-property deliveries as the window, broker header frame -> channel handler -> delivery/get/return emission as the corridor, and `(handler_decode_work, transient_io_objects, consumed_delivery_latency, live_consume_cpu)` as the lexicographic potential. `ContentHeader.decode_empty` now parses exact 14-byte empty-property header payloads directly and falls back to the generic decoder for non-empty or malformed payloads. The benchmark harness now labels zero-duration lanes and varies direct-decode payloads so release optimization cannot erase the stage loop. Evidence: `src/amqp/wire/amqp_zero_nine_one/content_header.cr`, `src/amqp/channel.cr`, `spec/wire/content_header_spec.cr`, `tools/perf_publish.cr`; focused content-header/channel/confirm specs, full default spec, release no-codegen perf build, format check, and live LavinMQ channel/subscription/confirm/stats specs pass; `.tmp/bench/current_ng_lavinmq_header_decode_20260518.json` reports `decode_empty_header_direct` ~872.0M ops/s vs `decode_empty_header_generic` ~30.2M ops/s. This is verified as read-side handler CPU reduction, not as a clean end-to-end throughput guarantee. Trust `{F:0.9,G:0.52,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: content-header payload shape, header decoder, channel delivery emission, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-074|performance] The benchmark harness now has a read-side corridor witness: `consume_no_ack_preloaded` preloads a named exclusive queue outside the timed window, then measures `basic.consume` plus `Subscription#receive` drain of no-ack deliveries. The first server-named auto-delete queue attempt was refuted because closing the consumer deleted the queue before cleanup and made `queue_purge` close the channel with broker 404. Evidence: `tools/perf_publish.cr`; release no-codegen build, format check, diff check, and full default spec pass; `.tmp/bench/current_ng_lavinmq_consume_lane_20260518.json` reports noisy LavinMQ 2.4.0 median ~247.3k msg/s over samples ~131.1k/~396.6k/~247.3k. This is a read-side baseline for future optimization, not a release guarantee. Trust `{F:0.86,G:0.45,R:0.82}`. Status: directionally verified 2026-05-18; decay trigger: benchmark harness, subscription delivery path, broker queue deletion semantics, broker version, compiler, or scheduler behavior changes.

- [LM-075|performance] The tenth higher-level LTP/WBA move is `Pending Body Buffer Reuse`: treat single-frame or empty inbound content bodies as the window, body frame -> pending body state -> delivery/get/return body bytes as the corridor, and `(per_delivery_io_memory_allocations, body_lifetime_correctness, consume_latency)` as the lexicographic potential. `emit_pending_delivery` now clears and reuses the pending `IO::Memory` buffer when the emitted body came from the direct single-frame path or is empty; multi-frame bodies still detach the old `IO::Memory` because the emitted `Bytes` slice must remain stable after delivery. Evidence: `src/amqp/channel.cr`; focused channel/subscription/stats specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ channel/subscription/stats specs pass; `.tmp/bench/current_ng_lavinmq_pending_body_reuse_20260518.json` reports noisy `consume_no_ack_preloaded` median ~251.5k msg/s. This is verified as allocation reduction plus behavior preservation, not a clean throughput guarantee. Trust `{F:0.9,G:0.5,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: pending body storage, delivery body lifetime, multi-frame assembly, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-076|performance] The eleventh higher-level LTP/WBA move is `Direct Settlement Writers`: treat per-delivery `ack`/`nack`/`reject` writes in manual-consume corridors as the window, user delivery settlement -> method frame write under the connection write mutex as the corridor, and `(settlement_payload_allocations, frame_object_allocations, wire_exactness, consume_ack_latency)` as the lexicographic potential. `BasicMethods.write_ack_frame`, `write_nack_frame`, and `write_reject_frame` now write fixed-size method frames directly, and `Channel#ack/#nack/#reject` use these writers. The harness now emits `consume_ack_preloaded` for manual-ack consume measurement. Evidence: `src/amqp/wire/amqp_zero_nine_one/basic_methods.cr`, `src/amqp/channel.cr`, `spec/wire/basic_methods_spec.cr`, `tools/perf_publish.cr`; focused wire/channel/subscription/stats specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ channel/subscription/stats specs pass; `.tmp/bench/current_ng_lavinmq_direct_ack_write_20260518.json` reports noisy `consume_ack_preloaded` median ~269.1k msg/s. This is verified as allocation/write-path reduction plus behavior preservation, not a clean throughput guarantee. Trust `{F:0.9,G:0.5,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: delivery settlement writer, connection write locking, manual consume ack semantics, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-077|performance] The twelfth higher-level LTP/WBA move is `Direct Deliver Parser`: treat incoming `basic.deliver` method payloads as the window, broker method frame -> channel handler -> pending content assembly as the corridor, and `(handler_io_memory_allocations, shortstr_temp_copies, method_parse_cpu, consume_latency)` as the lexicographic potential. `BasicMethods.decode_deliver_frame_payload` now parses well-formed full `basic.deliver` payloads directly and falls back to the existing generic decoder for non-deliver or malformed payloads. The harness now reports generic/direct `basic.deliver` parse stages. Evidence: `src/amqp/wire/amqp_zero_nine_one/basic_methods.cr`, `src/amqp/channel.cr`, `spec/wire/basic_methods_spec.cr`, `tools/perf_publish.cr`; focused wire/channel/subscription/stats specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ channel/subscription/stats specs pass; `.tmp/bench/current_ng_lavinmq_direct_deliver_parse_20260518.json` reports `parse_basic_deliver_direct` ~11.9M ops/s vs generic ~9.0M ops/s. This is verified as modest handler parse allocation/CPU reduction; string creation still dominates, and consume throughput remains noisy. Trust `{F:0.9,G:0.48,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: basic.deliver payload layout, channel content assembly, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-078|performance] The thirteenth higher-level LTP/WBA move is `Pending Body Container Reuse`: treat content-bearing method setup before content header/body frames as the window, method frame -> pending body state -> body assembly as the corridor, and `(per_delivery_io_memory_allocations, body_lifetime_correctness, consume_latency)` as the lexicographic potential. Content-bearing method handling now clears the existing pending `IO::Memory` body container instead of allocating a fresh container for every `basic.deliver`, `basic.get-ok`, or `basic.return`. Multi-frame bodies still detach their old buffer at emission, so delivered body slices remain stable; direct single-frame and empty bodies do not depend on that container. Evidence: `src/amqp/channel.cr`; focused channel/subscription/confirm/stats specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ channel/subscription/stats specs pass; `.tmp/bench/current_ng_lavinmq_pending_body_reset_20260518.json` reports noisy `consume_no_ack_preloaded` median ~213.0k msg/s and `consume_ack_preloaded` median ~382.3k msg/s. This is verified as allocation reduction plus body-lifetime preservation, not as a clean end-to-end throughput win. Trust `{F:0.9,G:0.5,R:0.86}`. Status: live-verified 2026-05-18; decay trigger: pending body storage, body slice lifetime, content assembly, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-079|performance] The fourteenth higher-level LTP/WBA move is `Public Bytes Publish Corridor`: treat public fire-and-forget `publish(exchange, routing_key, body : Bytes)` as the window, caller bytes -> publish frames as the corridor, and `(message_wrapper_allocations, recovery_semantics, live_publish_latency)` as the lexicographic potential. Non-confirm public bytes publish now writes directly from `Bytes` instead of first wrapping the body in `Amqp::Message`; confirm-mode bytes publish still uses the existing `Message` path so recovery/full replay semantics remain unchanged. The harness now reports `publish_single_bytes` separately from prebuilt-message `publish_single`. Evidence: `src/amqp/channel.cr`, `tools/perf_publish.cr`; focused API/channel/stats specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ channel/stats specs pass; `.tmp/bench/current_ng_lavinmq_single_bytes_20260518.json` reports `publish_single_bytes` median ~763.6k msg/s vs prebuilt-message `publish_single` ~731.7k and `publish_batch_bytes` ~836.1k. This is directionally verified as a public API allocation reduction, not a universal throughput guarantee. Trust `{F:0.88,G:0.52,R:0.86}`. Status: live-verified directionally 2026-05-18; decay trigger: public bytes publish API, confirm/recovery semantics, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-080|performance] The fifteenth higher-level LTP/WBA move is `Public Bytes Sync-Confirm Corridor`: treat public `publish_confirm(body : Bytes, ...)` under `Recovery::None` as the window, caller bytes -> pending confirm registration -> publish frames -> sync waiter as the corridor, and `(message_wrapper_allocations, replay_payload_correctness, sync_confirm_latency)` as the lexicographic potential. Sync confirm for `Bytes` now registers a nil replay payload under `Recovery::None` and writes directly from caller bytes; `Recovery::Full` still constructs a replay `Message`, preserving reconnect replay semantics. The harness now reports `confirm_sync_bytes` separately from prebuilt-message `confirm_sync`. Evidence: `src/amqp/channel.cr`, `tools/perf_publish.cr`; focused API/confirm/recovery specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ confirm/stats specs pass; `.tmp/bench/current_ng_lavinmq_confirm_bytes_20260518.json` reports `confirm_sync_bytes` median ~5.02k msg/s vs prebuilt-message `confirm_sync` ~4.94k. This is verified as a public API allocation reduction with small directional speed signal; sync confirms remain broker RTT dominated. Trust `{F:0.88,G:0.5,R:0.86}`. Status: live-verified directionally 2026-05-18; decay trigger: public bytes confirm API, pending confirm replay retention, recovery mode, benchmark shape, broker version, compiler, or scheduler behavior changes.

- [LM-081|performance] High-level LTP/WBA audit says the remaining high-value corridors are no longer codec micro-paths but API/transport/scheduler corridors. Ranked candidates: prepared fixed-route publisher, striped publisher pool, consumer fast lane, stats batching/opt-out, and explicit write coalescing. Prepared publisher is the first selected move because it removes repeated route method-frame selection without changing default ordering, recovery, or scheduler semantics. Evidence: code audit of `Connection#with_write`, reader dispatch, channel publish paths, subscription mailbox, and latest LavinMQ benchmark lanes. Trust `{F:0.78,G:0.58,R:0.78}`. Status: design anchor 2026-05-18; decay trigger: benchmark harness, publish API, connection write topology, scheduler behavior, or broker target changes.

- [LM-082|performance] The sixteenth higher-level LTP/WBA move is `Prepared Publisher Corridor`: treat repeated fixed-route fire-and-forget publishes as the window, prepared route/properties -> publish method/header/body frames as the corridor, and `(route_method_frame_selection, write_lock_work, recovery_semantics, live_publish_latency)` as the lexicographic potential. `Amqp::PreparedPublisher` and `Channel#prepared_publisher` now precompute the `basic.publish` method frame for non-confirm fixed-route publishes; confirm-mode channels fall back to the existing confirm paths so replay semantics remain unchanged. The harness now emits `publish_prepared_bytes` and `publish_prepared_batch_bytes` lanes. Evidence: `src/amqp/prepared_publisher.cr`, `src/amqp/channel.cr`, `tools/perf_publish.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused API/channel/stats spec, full default spec, docs falsifier-link spec, format check, diff check, release no-codegen perf build, and live LavinMQ API/channel/stats specs pass. Short LavinMQ release probe in `.tmp/bench/current_ng_lavinmq_prepared_publisher_20260518.json` reports `publish_prepared_batch_bytes` median ~764.6k msg/s vs `publish_batch_bytes` ~739.7k, but `publish_prepared_bytes` ~746.0k trails `publish_single_bytes` ~819.8k on this noisy run. This verifies a usable/measurable prepared corridor with batch-directional signal, not a universal single-publish win. Trust `{F:0.88,G:0.52,R:0.86}`. Status: live-verified directionally 2026-05-18; decay trigger: prepared publisher API, publish method-frame writer, confirm fallback semantics, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-083|performance] The seventeenth higher-level LTP/WBA move is `Prepared Properties Header Corridor`: treat repeated prepared publishes with fixed non-empty properties as the window, prepared property flags/tail -> body-size-specific content-header frame -> publish frames as the corridor, and `(property_tail_reencoding, header_frame_allocations_after_warmup, wire_exactness, fixed_properties_latency)` as the lexicographic potential. `ContentHeader.encode_properties` separates reusable property-list tails from body-size-dependent fields, and `PreparedPublisher` caches a full content-header frame after one repeated body size. Refuted branch: direct reusable-tail writing without caching the full header frame lost on the short live probe because it traded property encoding for more small buffered writes. Evidence: `src/amqp/wire/amqp_zero_nine_one/content_header.cr`, `src/amqp/prepared_publisher.cr`, `src/amqp/channel.cr`, `tools/perf_publish.cr`, `spec/wire/content_header_spec.cr`; focused wire/API/channel specs and live LavinMQ wire/channel/API/stats specs pass; `.tmp/bench/current_ng_lavinmq_prepared_props_cached_20260518.json` reports `publish_prepared_props_bytes` median ~762.7k msg/s vs `publish_single_props` ~684.2k. This verifies a fixed-properties prepared-publisher signal, not a general empty-properties publish claim. Trust `{F:0.86,G:0.48,R:0.84}`. Status: live-verified directionally 2026-05-18; decay trigger: content-header encoding, prepared publisher cache semantics, property model, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-084|performance] The eighteenth higher-level LTP/WBA move is `Consumer Lookup Corridor`: treat stable delivery streams for one or a few consumers on one channel as the window, `basic.deliver` consumer tag -> subscription mailbox as the corridor, and `(consumer_mutex_acquisitions, hash_lookups_per_delivery, route_correctness, consume_latency)` as the lexicographic potential. Channel delivery routing now uses a handler-local one-entry consumer lookup cache guarded by an atomic consumer-map generation; register/delete paths bump the generation, and the handler reuses the cached subscription only when generation and tag still match. Evidence: `src/amqp/channel.cr`, `spec/subscription_spec.cr`; focused channel/subscription/recovery/stats specs, full default spec, release no-codegen perf build, format check, live LavinMQ channel/subscription/recovery/stats specs, and live multi-consumer same-channel routing spec pass; `.tmp/bench/current_ng_lavinmq_consumer_cache_20260518.json` reports `consume_no_ack_preloaded` median ~392.0k msg/s and `consume_ack_preloaded` median ~463.8k msg/s. This verifies consumer hot-path lock/hash-work reduction with directional throughput signal; no-ack consume remains noisy. Trust `{F:0.88,G:0.5,R:0.84}`. Status: live-verified directionally 2026-05-18; decay trigger: consumer map lifecycle, channel handler threading, subscription mailbox semantics, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-085|performance] The nineteenth higher-level LTP/WBA move is `Confirm Stats Batching`: treat broker `multiple=true` confirm settlement as the window, confirm tag range -> settled publishes -> stats counters/callbacks as the corridor, and `(atomic_counter_ops_per_settlement, exact_counter_values, callback_ordering, confirm_latency)` as the lexicographic potential. `settle_publish` now accumulates ack/nack totals for multiple settlements and increments `confirmed_ack` / `confirmed_nack` once per non-zero total; per-publish callbacks and outcome channels remain unchanged. Refuted branch: changing all stats counters to relaxed atomic ordering is deferred because a Crystal 1.20.1 eval probe hit a compiler/runtime bug, so that needs a separate compiler-safe falsifier. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`; focused confirm/stats specs, full default spec, release no-codegen perf build, format check, and live LavinMQ confirm/stats specs pass; `.tmp/bench/current_ng_lavinmq_confirm_stats_batch_20260518.json` reports healthy confirm lanes, including `confirm_batch_wait` median ~28.9k msg/s and `confirm_window_500` median ~13.9k msg/s. This verifies atomic stats work reduction under multiple confirms, not a clean end-to-end throughput guarantee. Trust `{F:0.88,G:0.48,R:0.84}`. Status: live-verified 2026-05-18; decay trigger: confirm settlement, stats counters, callback dispatch, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-086|performance] The twentieth higher-level LTP/WBA move is `Dense Confirm-Range Settlement`: treat dense broker `multiple=true` confirm ranges as the window, low unconfirmed tag -> broker tag -> settled publish list as the corridor, and `(range_settlement_allocations, sort_work, sparse_range_safety, callback_ordering)` as the lexicographic potential. Dense multiple settlement now walks the monotonic confirm-tag range directly instead of building `@unconfirmed.select { ... }.sort`; sparse ranges keep the old select/sort strategy to avoid pathological loops after many individual settlements. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`; focused confirm/stats specs, full default spec, release no-codegen perf build, format check, live LavinMQ confirm/stats specs, and a sparse-hole multiple settlement guard pass; `.tmp/bench/current_ng_lavinmq_confirm_range_dense_20260518.json` reports healthy confirm lanes, including `confirm_batch_wait` median ~44.9k msg/s, `confirm_batch_wait_bytes` ~43.7k, and `confirm_window_500` ~15.3k. This verifies dense settlement allocation/sort-work reduction with noisy live throughput, not a hard performance guarantee. Trust `{F:0.88,G:0.5,R:0.84}`. Status: live-verified 2026-05-18; decay trigger: confirm tracker data structure, multiple ack/nack semantics, callback ordering, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-087|performance] The twenty-first higher-level LTP/WBA move is `Cached Deliver Strings`: treat stable `basic.deliver` streams where consumer tag, exchange, and routing key repeat as the window, broker deliver method payload -> pending delivery metadata as the corridor, and `(shortstr_allocations_per_delivery, malformed_payload_fallback, route_correctness, consume_latency)` as the lexicographic potential. Channel method handling now parses `basic.deliver` payloads with a channel-local shortstr cache, reusing repeated consumer-tag/exchange/routing-key strings after warmup; malformed or non-deliver payloads still fall back to the existing generic method path. Evidence: `src/amqp/channel.cr`; focused channel/subscription/stats/wire-basic specs, full default spec, release no-codegen perf build, format check, live LavinMQ channel/subscription/stats specs, and the existing multi-consumer same-channel routing guard pass; `.tmp/bench/current_ng_lavinmq_deliver_string_cache_20260518.json` reports `consume_no_ack_preloaded` median ~511.7k msg/s and `consume_ack_preloaded` median ~436.9k. This verifies delivery metadata allocation reduction with directional no-ack consume signal; ack consume remains noisy. Trust `{F:0.88,G:0.5,R:0.84}`. Status: live-verified directionally 2026-05-18; decay trigger: basic.deliver payload layout, channel method parser, delivery metadata lifetime, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-088|bugfix] Block-form `consume(auto_ack: true)` is AMQP no-ack and must not send a client `basic.ack` after the user block returns. The previous implementation did send that ack, contradicting `docs/09-consumer.md` and risking broker channel close on unknown delivery tags. The loop now leaves successful no-ack deliveries alone, rejects only manual-ack failures, and logs no-ack block exceptions as warning. Evidence: `src/amqp/channel.cr`, `docs/02-public-api.md`, `docs/09-consumer.md`, `spec/channel_spec.cr`; focused default channel/API/docs specs pass; live LavinMQ focused spec passes with a no-ack block-consume channel-liveness falsifier; live LavinMQ full suite exits 0 with 200 examples, 0 failures, 0 errors, 4 pending; format, perf no-codegen build, and diff check pass. Trust `{F:0.9,G:0.65,R:0.88}`. Status: live-verified 2026-05-18; decay trigger: block-form consume loop, auto_ack/no_ack mapping, broker delivery ack semantics, or consumer docs changes.

- [LM-089|performance] The twenty-second higher-level LTP/WBA move is `Empty Header Metadata Corridor`: treat incoming empty content-header payloads for default-property deliveries as the window, broker header frame -> channel handler -> pending body assembly as the corridor, and `(decoded_record_materialization, empty_properties_materialization, handler_decode_work, consume_latency)` as the lexicographic potential. `ContentHeader.decode_empty_metadata` now returns only `{class_id, body_size}` for exact empty-property headers, and `Channel#process_header_frame` uses that metadata directly; non-empty or malformed headers still fall back to the full decoder. Evidence: `src/amqp/wire/amqp_zero_nine_one/content_header.cr`, `src/amqp/channel.cr`, `spec/wire/content_header_spec.cr`, `tools/perf_publish.cr`; focused default wire/channel/subscription/stats specs pass; live LavinMQ focused and full suites pass; release no-codegen perf build, format check, and diff check pass; `.tmp/bench/current_ng_lavinmq_empty_header_metadata_20260518.json` reports `decode_empty_header_metadata` median ~897.3M ops/s vs `decode_empty_header_direct` ~870.3M and generic ~29.3M, with consume lanes still noisy. This verifies handler metadata/allocation reduction, not a hard end-to-end throughput guarantee. Trust `{F:0.88,G:0.48,R:0.84}`. Status: live-verified 2026-05-18; decay trigger: content-header payload layout, channel header handler, properties representation, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-090|performance] The twenty-third higher-level LTP/WBA move is `Empty Properties Emission Corridor`: treat empty-property deliveries after header metadata decode as the window, pending content state -> public delivery/get/return object as the corridor, and `(empty_properties_constructor_work, delivery_emission_work, body_lifetime_correctness, consume_latency)` as the lexicographic potential. `Channel#emit_pending_delivery` now uses a channel-level `EMPTY_PROPERTIES` value when the content header had no properties, instead of constructing a fresh empty `Properties` value at emission. Evidence: `src/amqp/channel.cr`; focused default channel/subscription/stats specs pass; live LavinMQ focused and sequential full suites pass; release no-codegen perf build, format check, and diff check pass; `.tmp/bench/current_ng_lavinmq_empty_props_const_20260518.json` reports noisy consume medians lower than the previous probe, so this is verified as behavior-preserving emission cleanup, not an end-to-end throughput win. Trust `{F:0.86,G:0.42,R:0.82}`. Status: live-verified 2026-05-18; decay trigger: properties representation, delivery/get/return object construction, content assembly, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-091|performance] The twenty-fourth higher-level LTP/WBA move is `Confirm Batch Empty-Properties Branch`: treat confirm-mode `publish_batch` / `publish_confirm_batch` for `Array(Message)` with empty properties as the window, pending confirm registration -> batch frame write under the connection write mutex as the corridor, and `(per_message_property_branch_work_under_write_lock, mixed_property_correctness, confirm_batch_latency)` as the lexicographic potential. Confirm batch message paths now pre-detect all-empty properties once per batch/window and use the empty-header writer directly for that branch, matching the existing unconfirmed batch shape; mixed-property batches keep the per-message encoder path and have a live broker falsifier. Evidence: `src/amqp/channel.cr`, `spec/confirms_spec.cr`; focused default confirm/channel/stats specs pass; live LavinMQ focused and full suites pass; release no-codegen perf build, format check, and diff check pass; `.tmp/bench/current_ng_lavinmq_confirm_batch_empty_branch_20260518.json` shows high confirm-batch sample variance, so this is verified as write-lock branch reduction plus mixed-property correctness, not as a hard throughput win. Trust `{F:0.88,G:0.46,R:0.84}`. Status: live-verified 2026-05-18; decay trigger: confirm batch implementation, message properties representation, write-lock topology, benchmark shape, broker version, compiler, or scheduler changes.

- [LM-092|bugfix] Raw-byte `publish_batch(Array(Bytes))` now applies the same open/flow gates as single publish and `publish_batch(Array(Message))` before taking the non-confirm fast path. The previous ordering returned through `publish_batch_unconfirmed` before `ensure_open!` / `wait_for_flow_active`, so it could bypass `channel.flow` and expose lower-level close behavior. Evidence: `src/amqp/channel.cr`, `spec/channel_spec.cr`; focused default channel/API specs pass; live LavinMQ focused spec passes with raw-byte batch flow and closed-channel guards; live LavinMQ full suite exits 0 with 203 examples, 0 failures, 0 errors, 4 pending; format, perf no-codegen build, and diff check pass. Trust `{F:0.9,G:0.65,R:0.88}`. Status: live-verified 2026-05-18; decay trigger: publish_batch overload ordering, channel.flow semantics, channel close behavior, or bytes batch fast path changes.

- [LM-093|bugfix] `basic_publish { |ok| ... }` callback mode now applies open/flow gates before registering and writing the confirm publish. The previous path auto-enabled confirms and then called `publish_registered_callback` directly, so already-confirmed channels could bypass `channel.flow` and expose lower-level close behavior. Evidence: `src/amqp/channel.cr`, `spec/channel_spec.cr`; focused default channel/confirm/API specs pass; live LavinMQ focused spec passes with callback publish flow and closed-channel guards; live LavinMQ full suite exits 0 with 205 examples, 0 failures, 0 errors, 4 pending; format, perf no-codegen build, and diff check pass. Trust `{F:0.9,G:0.65,R:0.88}`. Status: live-verified 2026-05-18; decay trigger: amqp-client compatibility callback publish path, confirm_select behavior, channel.flow semantics, or channel close behavior changes.

- [LM-094|bugfix] `Amqp::Message.new(String)` and `Amqp::Message.new(IO)` now copy into owned bytes, while `Amqp::Message.new(Bytes)` remains explicit zero-copy. The previous constructors stored `String#to_slice` bytes from caller-owned or temporary string storage, which made long-lived prebuilt messages depend on borrowed lifetime. Evidence: `src/amqp/message.cr`, `spec/message_spec.cr`, `docs/02-public-api.md`; focused message/API specs, full default spec, release no-codegen perf build, format check, diff check, and live LavinMQ message/API/channel/confirm specs pass. Trust `{F:0.9,G:0.65,R:0.88}`. Status: live-verified 2026-05-18; decay trigger: message constructors, publish batch replay retention, body ownership docs, or Crystal string/slice lifetime semantics changes.

- [LM-095|parity] NamedTuple `args` sugar is now accepted for public topology and consumer helper APIs and coerced into canonical `Amqp::Arguments`, while existing `Amqp::Arguments` values remain zero-copy. The coercion preserves supported scalar widths such as `Int16`, `Int32`, and `UInt8`, and recursively coerces nested arrays/tables. Evidence: `src/amqp/arguments.cr`, `src/amqp/channel.cr`, `src/amqp/queue.cr`, `src/amqp/exchange.cr`, `spec/arguments_spec.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused no-broker argument/API/channel specs pass; live LavinMQ topology/API smoke passes; full no-broker spec, release no-codegen perf build, format check, and diff check pass. Trust `{F:0.9,G:0.62,R:0.88}`. Status: live-verified 2026-05-18; decay trigger: arguments type model, topology APIs, consumer APIs, or AMQP field-width encoding changes.

- [LM-096|parity] Accepted `no_wait` compatibility overloads now emit broker no-wait frames instead of only accepting the parameter for source compatibility. Covered paths are queue declare, queue bind, exchange bind/unbind, and basic cancel through Channel and wrapper/facade APIs. Evidence: `src/amqp/channel.cr`, `src/amqp/queue.cr`, `src/amqp/exchange.cr`, `src/amqp-client.cr`, `src/amqp/wire/amqp_zero_nine_one/*_methods.cr`, `spec/wire/no_wait_methods_spec.cr`, `spec/api_surface_spec.cr`, `spec/channel_spec.cr`; focused wire/API specs pass; live LavinMQ no-wait/API/channel spec passes with 39 examples; full no-broker spec, release no-codegen perf build, format check, and diff check pass. Trust `{F:0.9,G:0.62,R:0.88}`. Status: live-verified 2026-05-18; decay trigger: topology method encoders, basic.cancel semantics, wrapper/facade APIs, or broker no-wait behavior changes.

- [LM-097|docs] The risk register now separates checked surfaces from opt-in release gates: no-broker CI covers default specs, format, dependency/docs drift, TLS wrong-SAN, and tool type-checks; broker CI covers plain-AMQP RabbitMQ/LavinMQ specs; manual release-gate CI covers RabbitMQ/LavinMQ TLS, backpressure, and Docker chaos; manual perf-smoke CI covers benchmark harness execution and positive JSON lane validation; `tools/perf_threshold_assert.cr` validates saved benchmark JSON against explicit host-specific threshold profiles. Normative `spec/perf/` runners, process-wide memory budgets, and actual queued-byte mailbox accounting remain explicit cutlines. Evidence: `docs/20-risk-register.md`, `.github/workflows/ci.yml`, `.github/workflows/release-gates.yml`, `.github/workflows/perf-smoke.yml`, `tools/perf_smoke_assert.cr`, `tools/perf_threshold_assert.cr`, `spec/perf_thresholds_spec.cr`, `spec/tls_spec.cr`, `spec/docs_falsifier_link_spec.cr`, `spec/wire/frame_spec.cr`, `spec/wire/types_spec.cr`, `spec/channel_spec.cr`, `spec/subscription_spec.cr`, `spec/chaos_spec.cr`. Trust `{F:0.9,G:0.75,R:0.9}`. Status: refreshed 2026-05-20; decay trigger: CI workflow, TLS fixture, docs guard, broker matrix, release-gate workflow, perf-smoke workflow, benchmark JSON schema, threshold profile schema, or malicious-peer caps changes.

- [LM-098|guard] `T-CODEC-PURE-001` is now executable as `spec/code_hygiene_spec.cr`: shard/spec source cannot contain direct private-ivar reach-in, and `src/amqp/wire` cannot introduce fibers, sockets, TLS/OpenSSL use, or class/module-variable state. Evidence: `spec/code_hygiene_spec.cr`, `docs/20-risk-register.md`; focused hygiene spec passes. Trust `{F:0.9,G:0.7,R:0.9}`. Status: verified 2026-05-18; decay trigger: hygiene spec changes, wire-codec directory move, or intentional codec dependency expansion.

- [LM-099|guard] `T-API-DEPS-001` is now executable as `spec/dependency_hygiene_spec.cr`: `shard.yml` is parsed with stdlib YAML and runtime dependencies must remain empty; an empty-dependency lockfile, if locally generated, must also lock no shards. Evidence: `spec/dependency_hygiene_spec.cr`, `shard.yml`, `docs/20-risk-register.md`; focused dependency spec passes on release and dev Crystal. Trust `{F:0.9,G:0.75,R:0.9}`. Status: verified 2026-05-18; decay trigger: shard dependency model, dev dependency policy, lockfile format, or CI workflow changes.

- [LM-100|guard] `T-API-NOGLOBAL-001` is now executable in `spec/code_hygiene_spec.cr`: implementation source cannot introduce module/class variables (`@@`). Current module-level state remains limited to logger constants such as `Log = ::Log.for(...)`. Evidence: `spec/code_hygiene_spec.cr`, `docs/01-design-principles.md`, `docs/16-falsifier-matrix.md`; focused hygiene spec and full no-broker spec pass. Trust `{F:0.9,G:0.75,R:0.9}`. Status: verified 2026-05-18; decay trigger: public namespace state policy, logger pattern, or source hygiene spec changes.

- [LM-101|guard] Deprecated stdlib API cleanup now covers the capture proxy: `tools/capture_proxy.cr` uses `Time.instant` instead of deprecated `Time.monotonic`, and `spec/code_hygiene_spec.cr` rejects `Time.monotonic` in `src`, `spec`, and `tools`. Evidence: `tools/capture_proxy.cr`, `spec/code_hygiene_spec.cr`; release/dev no-codegen capture proxy builds emit no deprecation warnings; full no-broker spec passes. Trust `{F:0.9,G:0.65,R:0.9}`. Status: verified 2026-05-18; decay trigger: Crystal deprecation set, capture tooling, or source hygiene spec changes.

- [LM-102|performance] The LavinMQ single-publish attribution corridor is now measurable in the checked-in harness. `tools/perf_publish.cr` emits empty-body, bytes-empty-body, prepared-empty-body, alternating-route, and repeat single-publish lanes, and its consume lanes choose a legal buffer under `max_subscription_mailbox_bytes` instead of tripping the default mailbox capacity guard. Evidence: `tools/perf_publish.cr`; release no-codegen build passes; a LavinMQ 2.4.0 release probe at `.tmp/bench/current_ng_lavinmq_single_stage_repeat_20260520.json` passes `tools/perf_smoke_assert.cr` with 28 metrics and 8 stages, records `consume_buffer: 1024`, and reports `publish_single` ~457.7k msg/s, `publish_single_repeat` ~447.3k, `publish_single_bytes_empty_body` ~704.7k, `publish_prepared_empty_body` ~656.8k, and `publish_single_alternating_routes` ~329.3k. This refutes a simple first-lane cold-start explanation and points the next corridor toward route stability plus body-frame/broker ingest attribution, not a blind Message-wrapper rewrite. Trust `{F:0.86,G:0.45,R:0.82}`. Status: directionally verified 2026-05-20; decay trigger: benchmark harness shape, mailbox budget defaults, publish route cache, body frame writer, broker version, compiler, or scheduler changes.

- [LM-103|performance] Route-fanout and body-size attribution are now explicit benchmark corridors. `tools/perf_publish.cr` accepts `AMQP_BENCH_ROUTE_COUNTS` for round-robin route lanes and `AMQP_BENCH_BODY_SIZES` for live body-size lanes plus synthetic `encode_empty_publish_frames_body_<n>` stages. Evidence: `tools/perf_publish.cr`; release no-codegen build passes; a LavinMQ 2.4.0 release probe at `.tmp/bench/current_ng_lavinmq_route_body_stage_20260520.json` passes `tools/perf_smoke_assert.cr` with 34 metrics and 12 stages and reports live body-size medians 0 bytes ~770.2k, 64 bytes ~575.6k, 256 bytes ~331.1k, 1024 bytes ~242.5k msg/s, while synthetic encode medians fall from ~11.9M ops/s at 0 bytes to ~2.48M ops/s at 1024 bytes. Route-fanout medians descend from 2 routes ~511.0k to 4 routes ~394.0k and 8 routes ~312.1k msg/s. This supports body/write-volume and route-locality as the next explanatory corridors; it does not justify a blind runtime rewrite without a lower-level socket/body-write move. Trust `{F:0.86,G:0.45,R:0.82}`. Status: directionally verified 2026-05-20; decay trigger: benchmark harness shape, route cache behavior, body frame writer, socket buffering, broker version, compiler, or scheduler changes.

- [LM-104|performance] Benchmark artifacts now have a run-local recommendation pass. `tools/perf_recommend.cr` reads `tools/perf_publish.cr` JSON and emits conservative advice only when intra-run ratios cross large thresholds: batch publish, prepared fixed-route publishers, route fanout sharding, or body-size reduction. Evidence: `tools/perf_recommendations.cr`, `tools/perf_recommend.cr`, `spec/perf_recommendations_spec.cr`; focused recommendation spec passes; the latest LavinMQ route/body artifact prints batch, prepared, route-fanout, and body-size recommendations. This is an interpretation tool, not an optimizer or normative perf gate. Trust `{F:0.86,G:0.5,R:0.86}`. Status: verified 2026-05-20; decay trigger: benchmark JSON schema, lane names, recommendation thresholds, or perf-smoke workflow changes.

- [LM-105|performance] Saved benchmark artifacts now have longitudinal comparison tooling. `tools/perf_compare.cr` compares matching `metrics` and `stages` medians between a baseline JSON and a current JSON, reports regressions/improvements/missing/new lanes, and can fail locally via `AMQP_BENCH_COMPARE_FAIL_REGRESSION_PCT`. Evidence: `tools/perf_compare_report.cr`, `tools/perf_compare.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec and no-codegen compare build pass; a CLI smoke comparing two saved LavinMQ artifacts reports new body/route lanes plus matching-lane regressions/improvements. This is a drift detector for saved artifacts, not a normative benchmark runner. Trust `{F:0.86,G:0.52,R:0.86}`. Status: verified 2026-05-20; decay trigger: benchmark JSON schema, lane names, threshold env contract, CI tool list, or `spec/perf/` runner introduction.

- [LM-106|performance] Benchmark artifacts now carry stable environment metadata for comparison context: schema version, Crystal version/description, and compile flags for release/threading modes. `tools/perf_compare.cr` warns when those fields differ or exist only on one side. Evidence: `tools/perf_publish.cr`, `tools/perf_compare_report.cr`, `tools/perf_compare.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec, synthetic compare CLI warning smoke, full no-broker spec, and no-codegen builds for publish/compare pass. No local broker was reachable for a live artifact emission in this slice. Trust `{F:0.84,G:0.5,R:0.82}`. Status: compile/spec-verified 2026-05-20; decay trigger: benchmark JSON schema, Crystal compile flags, compare metadata policy, or perf-smoke workflow changes.

- [LM-107|performance] Saved benchmark comparisons now warn on workload-shape drift, not only compiler/build metadata drift. `tools/perf_compare.cr` checks top-level fields for redacted URL, publish/confirm counts, batch size, samples, body size, stage iterations, channel/connection counts, confirm windows, route counts, body sweep sizes, and consume buffer. Evidence: `tools/perf_compare_report.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec and compare no-codegen build pass. This is a non-failing context guard for human interpretation, not a release threshold. Trust `{F:0.86,G:0.52,R:0.86}`. Status: verified 2026-05-20; decay trigger: benchmark JSON workload fields, compare metadata policy, or `spec/perf/` runner introduction.

- [LM-108|performance] Benchmark metadata warnings can now become strict local gate failures. `tools/perf_compare.cr` accepts `AMQP_BENCH_COMPARE_FAIL_METADATA=1`; when set, any environment/workload metadata warning makes the command exit nonzero, while the default remains warning-only. Evidence: `tools/perf_compare.cr`, `tools/perf_compare_report.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec, compare no-codegen build, and synthetic CLI failure smoke pass. Trust `{F:0.86,G:0.55,R:0.86}`. Status: verified 2026-05-20; decay trigger: compare CLI env contract, metadata warning policy, or perf gate workflow changes.

- [LM-109|performance] Baseline lanes missing from a current benchmark artifact can now become strict local gate failures. `tools/perf_compare.cr` accepts `AMQP_BENCH_COMPARE_FAIL_MISSING_CURRENT=1`; when set, any `MissingCurrent` metric or stage makes the command exit nonzero, while new current-only lanes remain informational. Evidence: `tools/perf_compare.cr`, `tools/perf_compare_report.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec, compare no-codegen build, and synthetic CLI failure smoke pass. Trust `{F:0.86,G:0.55,R:0.86}`. Status: verified 2026-05-20; decay trigger: compare CLI env contract, benchmark lane schema, or perf gate workflow changes.

- [LM-110|performance] New current-only benchmark lanes can now become strict local gate failures. `tools/perf_compare.cr` accepts `AMQP_BENCH_COMPARE_FAIL_NEW_LANE=1`; when set, any `NewLane` metric or stage makes the command exit nonzero, while default exploratory comparison keeps new lanes informational. Evidence: `tools/perf_compare.cr`, `tools/perf_compare_report.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec, compare no-codegen build, and synthetic CLI failure smoke pass. Trust `{F:0.86,G:0.55,R:0.86}`. Status: verified 2026-05-20; decay trigger: compare CLI env contract, benchmark lane schema, or perf gate workflow changes.

- [LM-111|performance] Benchmark comparison strict mode now bundles context/schema drift guards. `tools/perf_compare.cr` accepts `AMQP_BENCH_COMPARE_STRICT=1`, enabling metadata-warning, missing-current-lane, and new-lane failures together while leaving regression percentage gating opt-in through `AMQP_BENCH_COMPARE_FAIL_REGRESSION_PCT`. Evidence: `tools/perf_compare.cr`; compare no-codegen build and synthetic strict CLI failure smoke pass. Trust `{F:0.84,G:0.55,R:0.84}`. Status: verified 2026-05-20; decay trigger: compare CLI env contract, strict-mode policy, or perf gate workflow changes.

- [LM-112|performance] Matching benchmark lane unit drift now enters the metadata warning/failure path. `tools/perf_compare.cr` warns when common metric/stage lanes have different `unit` values or a unit exists only on one side; aggregate strict mode fails such comparisons through metadata strict gating. Evidence: `tools/perf_compare_report.cr`, `spec/perf_compare_report_spec.cr`; focused compare spec, compare no-codegen build, and synthetic strict CLI failure smoke pass. Trust `{F:0.86,G:0.55,R:0.86}`. Status: verified 2026-05-20; decay trigger: benchmark JSON lane metadata, compare metadata warning policy, or perf gate workflow changes.

- [LM-113|performance] `T-PERF-PUB-001` now has the first default-off executable `spec/perf` carrier. `spec/perf/t_perf_pub_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, measures fire-and-forget publish throughput over warm-up/measurement windows, checks `AMQP_PERF_PUB_001_MIN`, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: `spec/perf/perf_spec_helper.cr`, `spec/perf/t_perf_pub_001_spec.cr`, docs updates; focused perf spec pending path passes, and the forced-live/no-broker path remains pending instead of failing. Trust `{F:0.84,G:0.45,R:0.84}`. Status: verified default-off 2026-05-20; decay trigger: perf spec env contract, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-114|performance] `T-PERF-PUB-002` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_pub_002_spec.cr` is gated by `AMQP_PERF_LIVE=1`, measures async publish-confirm throughput with a concurrent outcome-drain fiber, checks `AMQP_PERF_PUB_002_MIN`, rejects nacks/returns, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: `spec/perf/t_perf_pub_002_spec.cr`, docs updates; focused perf spec pending path passes, and the forced-live/no-broker path remains pending instead of failing. Trust `{F:0.84,G:0.45,R:0.84}`. Status: verified default-off 2026-05-20; decay trigger: perf spec env contract, async confirm API, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-115|performance] `T-PERF-MULTICHAN-001` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_multichan_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, measures same-connection multi-channel publish throughput with `AMQP_PERF_MULTICHAN_COUNT` channels/fibers, checks `AMQP_PERF_MULTICHAN_001_MIN`, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: `spec/perf/t_perf_multichan_001_spec.cr`, docs updates; focused perf spec pending path passes, and the forced-live/no-broker path remains pending instead of failing. Trust `{F:0.84,G:0.45,R:0.84}`. Status: verified default-off 2026-05-20; decay trigger: perf spec env contract, connection write serialization, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-116|performance] `T-PERF-CONS-001` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_cons_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, preloads warm-up and measured messages outside the timed section, drains deliveries with `Subscription#receive` plus inline `ack`, checks `AMQP_PERF_CONS_001_MIN`, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: `spec/perf/t_perf_cons_001_spec.cr`, `spec/perf/perf_spec_helper.cr`, docs updates; focused perf+docs guard exits 0 with 8 examples, 0 failures, 0 errors, 4 pending; forced-live/no-broker path remains pending instead of failing; release no-codegen compile, format check, and full no-broker suite pass with 257 examples, 0 failures, 0 errors, 96 pending. Trust `{F:0.86,G:0.45,R:0.86}`. Status: verified default-off 2026-05-20; decay trigger: perf spec env contract, subscription receive/ack semantics, mailbox budget policy, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-117|performance] `T-PERF-HANDSHAKE-001` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_handshake_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, records p99 over sequential plain AMQP connections, optionally records TLS p99 when `AMQP_TLS_URL` is set, checks caller-provided latency bounds, and writes ignored JSON/TXT results under `spec/perf/results/`. `PerfSpecHelper.redacted_url` also keeps perf result metadata from storing full credential-bearing broker URLs. Evidence: focused perf+docs guard exits 0 with 10 examples, 0 failures, 0 errors, 6 pending; forced-live/no-broker path remains pending instead of failing; release no-codegen compile, format check, and full no-broker suite pass with 259 examples, 0 failures, 0 errors, 98 pending. Trust `{F:0.86,G:0.45,R:0.86}`. Status: verified default-off 2026-05-20; decay trigger: perf spec env contract, connection handshake semantics, TLS env contract, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-118|performance] `T-PERF-STATS-001` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_stats_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, measures `Amqp::Stats#snapshot` average microseconds per call over `AMQP_PERF_STATS_READS`, requires no broker, checks `AMQP_PERF_STATS_001_MAX_US`, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: focused perf+docs guard exits 0 with 11 examples, 0 failures, 0 errors, 7 pending; forced local run exits 0 with 1 example, 0 failures, 0 errors, 0 pending and writes ignored local artifacts; release no-codegen compile, format check, and full no-broker suite pass with 260 examples, 0 failures, 0 errors, 99 pending. Trust `{F:0.86,G:0.45,R:0.86}`. Status: verified default-off/local 2026-05-23; decay trigger: stats counter layout, snapshot implementation, perf spec env contract, result artifact format, or normalized perf CI introduction.

- [LM-119|performance] `T-PERF-MEM-001..002` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_mem_001_002_spec.cr` is gated by `AMQP_PERF_LIVE=1`, measures idle live connection heap bytes per connection and buffered unread no-ack delivery heap bytes per delivery using `GC.stats.heap_size` deltas after collection, checks caller-provided byte bounds, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: focused perf+docs guard exits 0 with 13 examples, 0 failures, 0 errors, 9 pending; forced-live/no-broker path remains pending instead of failing; release no-codegen compile, format check, and full no-broker suite pass with 262 examples, 0 failures, 0 errors, 101 pending. Trust `{F:0.86,G:0.45,R:0.86}`. Status: verified default-off 2026-05-23; decay trigger: GC stats API, connection/channel object graph, subscription mailbox behavior, perf spec env contract, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-120|performance] `T-PERF-GC-001` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_gc_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, measures fire-and-forget publish allocation pressure through the current Crystal `GC.stats.total_bytes` delta, checks `AMQP_PERF_GC_001_MAX_BYTES_PER_MESSAGE`, and writes ignored JSON/TXT results under `spec/perf/results/`. The performance roadmap was corrected away from the unavailable `GC.stats.collections` field on Crystal 1.20.2. Evidence: focused perf+docs guard exits 0 with 14 examples, 0 failures, 0 errors, 10 pending; forced-live/no-broker path remains pending instead of failing; release no-codegen compile, format check, and full no-broker suite pass with 263 examples, 0 failures, 0 errors, 102 pending. Trust `{F:0.86,G:0.45,R:0.86}`. Status: verified default-off 2026-05-23; decay trigger: GC stats API, publish allocation path, perf spec env contract, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-121|performance] `T-PERF-RECOV-001` now has a default-off executable `spec/perf` carrier. `spec/perf/t_perf_recov_001_spec.cr` is gated by `AMQP_PERF_LIVE=1`, `AMQP_CHAOS_DOCKER_CONTAINER`, Docker availability, and broker reachability; it restarts the named broker container, waits for recovery, measures elapsed time to first successful `publish_confirm`, checks `AMQP_PERF_RECOV_001_MAX_MS`, and writes ignored JSON/TXT results under `spec/perf/results/`. Evidence: focused perf+docs guard exits 0 with 15 examples, 0 failures, 0 errors, 11 pending; forced-live/no-container path remains pending instead of failing; release no-codegen compile, format check, and full no-broker suite pass with 264 examples, 0 failures, 0 errors, 103 pending. Trust `{F:0.86,G:0.45,R:0.86}`. Status: verified default-off 2026-05-23; decay trigger: recovery state machine, Docker restart harness, publish_confirm semantics, perf spec env contract, result artifact format, broker reachability policy, or normalized perf CI introduction.

- [LM-122|performance] The twenty-fifth higher-level LTP/WBA move is `Async Bytes Confirm Corridor`: treat public `publish_async(body : Bytes, ...)` under confirm mode as the window, caller bytes -> pending confirm registration -> publish frames -> async outcome channel as the corridor, and `(message_wrapper_allocations, replay_payload_correctness, async_confirm_latency)` as the lexicographic potential. Bytes async confirms now write directly from `Bytes` and register a nil replay payload under `Recovery::None`; `Recovery::Full` still constructs a replay `Message` so reconnect replay semantics remain unchanged. Evidence: focused confirm/API/recovery specs pass with 58 examples, 0 failures, 0 errors, 49 pending; focused format check passes. The local broker was unreachable during this slice, so this is verified as allocation-path cleanup plus behavior preservation, not as an end-to-end throughput claim. Trust `{F:0.84,G:0.5,R:0.82}`. Status: compile/spec-verified 2026-05-23; decay trigger: publish_async bytes API, pending confirm replay retention, recovery mode, async confirm outcome handling, broker version, compiler, or scheduler changes.

## Known Risks

- [RISK-001|recovery] `Recovery::Full` remains the largest semantic-risk area: replay can duplicate messages, topology replay can partially fail, and consumer tags may conflict after partitions. Server-named queue remap, default-exchange pending publish replay, caller-close finality, topology/consumer replay fail-closed behavior, duplicate-delivery caveat, and explicit consumer-tag replay are now covered. Status: active residual distributed-systems risk. Trust `{F:0.84,G:0.8,R:0.86}`.

- [RISK-002|consumer] Consumer backpressure first stalls the per-channel handler, not the entire connection reader. Whole-connection backpressure remains possible only if the blocked channel's frame inbox fills. Status: partially mitigated by docs correction and opt-in live harness; eventual saturation coverage remains active. Trust `{F:0.7,G:0.7,R:0.7}`.

- [RISK-003|confirms] The named deterministic publisher-confirm cleanup gaps are now covered: multiple ack/nack, returned-before-ack, timeout then late ack, close while blocked, unread async outcome, batch sequence registration, mode-aware replay-payload retention, low-watermark progress tracking, sync-waiter cleanup, and concurrent sync confirms on one channel. Residual risk is real-broker rare ordering under load, not missing local falsifiers. Status: mitigated with residual live/perf risk. Trust `{F:0.87,G:0.7,R:0.9}`.

- [RISK-004|performance] Concrete performance targets are still not release guarantees, but the risk is lower now that `tools/perf_publish.cr` is checked in, direct writer optimizations have byte-equivalence witnesses, recovery-disabled confirm tracking no longer retains replay bodies for sync/async bytes paths, socket throughput defaults are configurable and old-client-aligned, and the harness can distinguish synthetic frame encoding, generic/direct ack parsing, confirm-window ladders, live multi-channel, shared-queue multi-connection, separate-queue multi-connection, prebuilt-message batch, bytes-batch publish throughput, live single-publish body/route/cache attribution, route-fanout and body-size slopes, sync confirm, and batch confirm+wait throughput. `tools/perf_recommend.cr` turns those lanes into conservative run-local guidance, and `tools/perf_compare.cr` compares saved artifacts against baselines for drift while warning on schema/compiler/build-mode/workload/lane-unit metadata drift; strict gates can promote those warnings to failures with `AMQP_BENCH_COMPARE_FAIL_METADATA=1`, reject disappeared baseline lanes with `AMQP_BENCH_COMPARE_FAIL_MISSING_CURRENT=1`, reject unreviewed new lanes with `AMQP_BENCH_COMPARE_FAIL_NEW_LANE=1`, or enable all three context/schema gates plus unit-drift gating with `AMQP_BENCH_COMPARE_STRICT=1`. All `T-PERF-*` roadmap rows now have default-off `spec/perf` carriers, but normalized CI baselines are still absent. Local numbers remain directional because broker/compiler/load variance is uncontrolled and `amqp-client.cr` parity is still from scratch/local harnesses, but paired release runs now show fire-and-forget and sync-confirm parity/slight lead on this host. Status: partially mitigated by hot-path cleanup, batch API, checked-in witness harness, mode-aware confirm retention, socket tuning, attribution probes, unconfirmed-publish lock elision, bytes-batch fast path, sync-confirm waiter cleanup, async-bytes replay cleanup, parser attribution, confirm-window sweep, single-publish attribution lanes, route/body sweep lanes, recommendation tooling, saved-artifact comparison tooling, artifact metadata warnings, strict metadata gating, strict missing-lane gating, aggregate strict gate, default-off perf carriers for every roadmap row, and roadmap downgrade. Trust `{F:0.87,G:0.5,R:0.85}`.

## Refuted Or Corrected Claims

- [REFUTE-001|grok-acp] "Grok ACP itself is unusable" is refuted for simple stdio tasks. Evidence: `grok_review` returned `OK`; `grok_worker` executed `pwd` and returned `/Users/sergey/Projects/Crystal/amqp-ng` after wrapper fixes. Trust `{F:0.9,G:0.5,R:0.9}`. Status: verified for minimal tasks only; decay trigger: Grok CLI/auth/config change.

## Verification Commands

Use these first when resuming:

```sh
git status --short
rg --files
crystal spec
crystal tool format --check src spec
```
